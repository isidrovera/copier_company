<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Integración con menú de navegación del portal - Breadcrumbs -->
    <template id="portal_breadcrumbs_copier" name="Portal layout : copier equipment breadcrumbs" inherit_id="portal.portal_breadcrumbs" priority="50">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'equipment' or page_name == 'equipment_detail' or page_name == 'equipment_counters'" t-attf-class="breadcrumb-item #{'active ' if page_name == 'equipment' else ''}">
                <a t-if="page_name in ['equipment_detail', 'equipment_counters']" t-attf-href="/my/copier/equipments?{{ keep_query() }}">Mis Equipos</a>
                <t t-else="">Mis Equipos</t>
            </li>
            <li t-if="page_name == 'equipment_detail' and equipment" class="breadcrumb-item active text-truncate">
                <span t-field="equipment.name.name"/> (<span t-field="equipment.serie_id"/>)
            </li>
            <li t-if="page_name == 'equipment_counters' and equipment" class="breadcrumb-item">
                <a t-attf-href="/my/copier/equipment/{{equipment.id}}"><span t-field="equipment.name.name"/></a>
            </li>
            <li t-if="page_name == 'equipment_counters'" class="breadcrumb-item active">
                Lecturas
            </li>
        </xpath>
    </template>

    <!-- Integración con el home del portal -->
    <template id="portal_my_home_copier_equipment" name="Mis Equipos en el Portal" customize_show="True" inherit_id="portal.portal_my_home" priority="50">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
            <t t-set="portal_service_category_enable" t-value="True"/>
        </xpath>
        <div id="portal_service_category" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="icon" t-value="'/copier_company/static/src/img/printer.svg'"/>
                <t t-set="title">Mis Equipos</t>
                <t t-set="text">Consulta los equipos que tienes en alquiler</t>
                <t t-set="url" t-value="'/my/copier/equipments'"/>
                <t t-set="placeholder_count" t-value="'equipment_count'"/>
            </t>
        </div>
    </template>
    

    <!-- Template para la página de listado de equipos en formato LISTA -->
<template id="portal_my_copier_equipments" name="My Rental Equipments">
    <t t-call="portal.portal_layout">
        <t t-set="breadcrumbs_searchbar" t-value="True"/>
        <t t-call="portal.portal_searchbar">
            <t t-set="title">Mis Equipos en Alquiler</t>
        </t>
        <t t-if="not equipments">
            <div class="alert alert-info text-center">
                <h4>No tienes equipos en alquiler actualmente</h4>
            </div>
        </t>
        <t t-if="equipments">
            <!-- Tabla de equipos en formato lista -->
            <div class="table-responsive shadow-sm mt-4">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr class="bg-light">
                            <th>Equipo</th>
                            <th>Marca</th>
                            <th>Serie</th>
                            <th>Tipo</th>
                            <th>Estado</th>
                            <th>Duración</th>
                            <th>Desde</th>
                            <th>Hasta</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr t-foreach="equipments" t-as="equipment">
                            <td>
                                <span t-field="equipment.name.name"/>
                            </td>
                            <td>
                                <span t-field="equipment.marca_id.name"/>
                            </td>
                            <td>
                                <span t-field="equipment.serie_id"/>
                            </td>
                            <td>
                                <span t-if="equipment.tipo == 'monocroma'">Blanco y negro</span>
                                <span t-else="">Color</span>
                            </td>
                            <td>
                                <span t-field="equipment.estado_maquina_id.name" t-attf-class="badge rounded-pill bg-{{ equipment.estado_maquina_id.name == 'Disponible' and 'success' or 'info' }}"/>
                            </td>
                            <td>
                                <span t-field="equipment.duracion_alquiler_id.name"/>
                            </td>
                            <td>
                                <span t-field="equipment.fecha_inicio_alquiler"/>
                            </td>
                            <td>
                                <span t-field="equipment.fecha_fin_alquiler"/>
                            </td>
                            <td class="text-center">
                                <div class="btn-group">
                                    <a t-att-href="'/my/copier/equipment/%s' % equipment.id" class="btn btn-sm btn-primary">
                                        <i class="fa fa-eye me-1"/>Ver
                                    </a>
                                    <a t-att-href="'/my/copier/equipment/%s/ticket' % equipment.id" class="btn btn-sm btn-danger">
                                        <i class="fa fa-ticket me-1"/>Ticket
                                    </a>
                                </div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </t>
    </t>
</template>
    
    <!-- Template para la página de detalle de un equipo -->
    <template id="portal_my_copier_equipment" name="My Rental Equipment Details">
    <t t-call="portal.portal_layout">
        <t t-set="wrapwrap_classes" t-value="'o_portal_bg_dark'"/>
        
        <style>
            @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&amp;display=swap');
            @import url('https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css');
            
            * { font-family: 'Inter', sans-serif; }
            
            .hero-equipment {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                border-radius: 0 0 30px 30px;
                margin: -20px -15px 0;
                padding: 40px 15px;
                position: relative;
                overflow: hidden;
            }
            
            .hero-equipment::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 100" fill="white" opacity="0.1"><polygon points="1000,100 1000,0 0,100"/></svg>');
                background-size: cover;
            }
            
            .equipment-card {
                background: white;
                border-radius: 20px;
                box-shadow: 0 20px 60px rgba(0,0,0,0.1);
                border: none;
                transition: all 0.3s ease;
                margin-top: -40px;
                position: relative;
                z-index: 10;
            }
            
            .equipment-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 25px 80px rgba(0,0,0,0.15);
            }
            
            .action-card {
                background: linear-gradient(135deg, #fff, #f8fafc);
                border: 2px solid #e2e8f0;
                border-radius: 16px;
                padding: 20px;
                text-align: center;
                transition: all 0.3s ease;
                text-decoration: none;
                color: inherit;
                display: block;
                height: 100%;
            }
            
            .action-card:hover {
                transform: translateY(-8px);
                box-shadow: 0 15px 40px rgba(0,0,0,0.1);
                border-color: #3b82f6;
                text-decoration: none;
                color: #1f2937;
            }
            
            .action-icon {
                width: 60px;
                height: 60px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                margin: 0 auto 15px;
                font-size: 24px;
                color: white;
                transition: all 0.3s ease;
            }
            
            .action-card:hover .action-icon {
                transform: scale(1.1);
            }
            
            .bg-gradient-primary { background: linear-gradient(135deg, #3b82f6, #1e40af) !important; }
            .bg-gradient-success { background: linear-gradient(135deg, #10b981, #047857) !important; }
            .bg-gradient-warning { background: linear-gradient(135deg, #f59e0b, #d97706) !important; }
            .bg-gradient-danger { background: linear-gradient(135deg, #ef4444, #dc2626) !important; }
            .bg-gradient-info { background: linear-gradient(135deg, #06b6d4, #0891b2) !important; }
            .bg-gradient-purple { background: linear-gradient(135deg, #8b5cf6, #7c3aed) !important; }
            
            .info-card {
                background: white;
                border-radius: 16px;
                box-shadow: 0 4px 20px rgba(0,0,0,0.08);
                border: 1px solid #e5e7eb;
                margin-bottom: 20px;
                overflow: hidden;
            }
            
            .info-card-header {
                background: linear-gradient(135deg, #f8fafc, #e2e8f0);
                padding: 20px;
                border-bottom: 1px solid #e5e7eb;
            }
            
            .info-row {
                padding: 12px 20px;
                border-bottom: 1px solid #f3f4f6;
                transition: background 0.2s ease;
            }
            
            .info-row:last-child {
                border-bottom: none;
            }
            
            .info-row:hover {
                background: #f9fafb;
            }
            
            .qr-card {
                background: linear-gradient(135deg, #f0f9ff, #e0f2fe);
                border: 2px solid #0ea5e9;
                border-radius: 16px;
                text-align: center;
                padding: 20px;
            }
            
            .status-badge {
                padding: 8px 16px;
                border-radius: 20px;
                font-weight: 600;
                font-size: 0.875rem;
            }
            
            .equipment-image {
                max-width: 100%;
                height: auto;
                border-radius: 16px;
                box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            }
            
            @media (max-width: 768px) {
                .hero-equipment {
                    margin: -20px -15px 0;
                    border-radius: 0 0 20px 20px;
                    padding: 30px 15px;
                }
                
                .equipment-card {
                    margin-top: -20px;
                }
            }
        </style>
        
        <!-- Hero Section -->
        <div class="hero-equipment animate__animated animate__fadeInDown">
            <div class="container position-relative">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h1 class="display-6 fw-bold mb-2">
                            <i class="fa fa-print me-3"></i>
                            <span t-field="equipment.name.name"/>
                        </h1>
                        <p class="lead mb-0 opacity-90">
                            Serie: <span t-field="equipment.serie_id"/> | 
                            <span t-if="equipment.tipo == 'monocroma'">Blanco y Negro</span>
                            <span t-else="">Color</span>
                        </p>
                    </div>
                    <div class="col-md-4 text-end">
                        <div class="status-badge bg-success text-white">
                            <i class="fa fa-check-circle me-2"></i>
                            Equipo Activo
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="container mt-4">
            <div class="row">
                <!-- Panel de Acciones -->
                <div class="col-lg-4 mb-4">
                    <div class="equipment-card animate__animated animate__fadeInLeft">
                        <div class="card-body p-4">
                            <h5 class="fw-bold mb-4 text-center">
                                <i class="fa fa-bolt me-2 text-primary"></i>
                                Acciones Rápidas
                            </h5>
                            
                            <div class="row g-3">
                                <!-- Reportar Contadores -->
                                <div class="col-12">
                                    <a t-attf-href="/public/upload_counters?copier_company_id=#{equipment.id}" class="action-card">
                                        <div class="action-icon bg-gradient-primary">
                                            <i class="fa fa-chart-bar"></i>
                                        </div>
                                        <h6 class="fw-semibold mb-2">Reportar Contadores</h6>
                                        <p class="text-muted small mb-0">Envía las lecturas mensuales</p>
                                    </a>
                                </div>
                                
                                <!-- Solicitar Toner -->
                                <div class="col-12">
                                    <a t-attf-href="/public/request_toner?copier_company_id=#{equipment.id}" class="action-card">
                                        <div class="action-icon bg-gradient-success">
                                            <i class="fa fa-tint"></i>
                                        </div>
                                        <h6 class="fw-semibold mb-2">Solicitar Toner</h6>
                                        <p class="text-muted small mb-0">Pide reposición de consumibles</p>
                                    </a>
                                </div>
                                
                                <!-- Asistencia Remota -->
                                <div class="col-12">
                                    <a t-attf-href="/public/remote_assistance?copier_company_id=#{equipment.id}" class="action-card">
                                        <div class="action-icon bg-gradient-info">
                                            <i class="fa fa-desktop"></i>
                                        </div>
                                        <h6 class="fw-semibold mb-2">Asistencia Remota</h6>
                                        <p class="text-muted small mb-0">Solicita ayuda técnica</p>
                                    </a>
                                </div>
                                
                                <!-- Reportar Problema -->
                                <div class="col-12">
                                    <a t-attf-href="/public/helpdesk_ticket?copier_company_id=#{equipment.id}" class="action-card">
                                        <div class="action-icon bg-gradient-danger">
                                            <i class="fa fa-exclamation-triangle"></i>
                                        </div>
                                        <h6 class="fw-semibold mb-2">Reportar Problema</h6>
                                        <p class="text-muted small mb-0">Reporta fallas o incidencias</p>
                                    </a>
                                </div>
                                
                                <!-- Ver Lecturas -->
                                <div class="col-12">
                                    <a t-att-href="'/my/copier/equipment/%s/counters' % equipment.id" class="action-card">
                                        <div class="action-icon bg-gradient-purple">
                                            <i class="fa fa-history"></i>
                                        </div>
                                        <h6 class="fw-semibold mb-2">Historial</h6>
                                        <p class="text-muted small mb-0">Ver lecturas anteriores</p>
                                    </a>
                                </div>
                                
                                <!-- Contacto Directo -->
                                <div class="col-12">
                                    <a t-attf-href="/public/send_whatsapp?copier_company_id=#{equipment.id}" class="action-card">
                                        <div class="action-icon bg-gradient-warning">
                                            <i class="fab fa-whatsapp"></i>
                                        </div>
                                        <h6 class="fw-semibold mb-2">WhatsApp</h6>
                                        <p class="text-muted small mb-0">Contacto directo</p>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Código QR -->
                    <div class="qr-card animate__animated animate__fadeInLeft animate__delay-1s" t-if="equipment.qr_code">
                        <h5 class="fw-bold mb-3 text-primary">
                            <i class="fa fa-qrcode me-2"></i>
                            Código QR
                        </h5>
                        <img class="img-fluid mb-3" t-att-src="image_data_uri(equipment.qr_code)" alt="Código QR" style="max-width: 200px;"/>
                        <p class="text-muted mb-0">Escanea para acceso rápido</p>
                    </div>
                </div>
                
                <!-- Información Principal -->
                <div class="col-lg-8">
                    <div class="row">
                        <!-- Detalles del Equipo -->
                        <div class="col-12 mb-4">
                            <div class="info-card animate__animated animate__fadeInRight">
                                <div class="info-card-header">
                                    <h5 class="fw-bold mb-0 text-primary">
                                        <i class="fa fa-info-circle me-2"></i>
                                        Detalles del Equipo
                                    </h5>
                                </div>
                                <div class="card-body p-0">
                                    <div class="row">
                                        <!-- Imagen del equipo -->
                                        <div class="col-md-4 p-4 text-center" t-if="equipment.imagen_id">
                                            <img class="equipment-image" t-att-src="image_data_uri(equipment.imagen_id)" alt="Imagen de la Máquina"/>
                                        </div>
                                        
                                        <!-- Datos básicos -->
                                        <div t-att-class="equipment.imagen_id and 'col-md-8 p-0' or 'col-md-12 p-0'">
                                            <div class="info-row">
                                                <div class="row">
                                                    <div class="col-sm-4 fw-semibold text-muted">Marca:</div>
                                                    <div class="col-sm-8" t-field="equipment.marca_id.name"/>
                                                </div>
                                            </div>
                                            <div class="info-row">
                                                <div class="row">
                                                    <div class="col-sm-4 fw-semibold text-muted">Serie:</div>
                                                    <div class="col-sm-8 fw-mono" t-field="equipment.serie_id"/>
                                                </div>
                                            </div>
                                            <div class="info-row">
                                                <div class="row">
                                                    <div class="col-sm-4 fw-semibold text-muted">Tipo:</div>
                                                    <div class="col-sm-8">
                                                        <span t-if="equipment.tipo == 'monocroma'" class="badge bg-secondary">Blanco y Negro</span>
                                                        <span t-else="" class="badge bg-primary">Color</span>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="info-row">
                                                <div class="row">
                                                    <div class="col-sm-4 fw-semibold text-muted">Formato:</div>
                                                    <div class="col-sm-8" t-field="equipment.formato"/>
                                                </div>
                                            </div>
                                            <div class="info-row">
                                                <div class="row">
                                                    <div class="col-sm-4 fw-semibold text-muted">Ubicación:</div>
                                                    <div class="col-sm-8">
                                                        <i class="fa fa-map-marker-alt text-warning me-2"></i>
                                                        <span t-field="equipment.ubicacion"/>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="info-row" t-if="equipment.sede">
                                                <div class="row">
                                                    <div class="col-sm-4 fw-semibold text-muted">Sede:</div>
                                                    <div class="col-sm-8" t-field="equipment.sede"/>
                                                </div>
                                            </div>
                                            <div class="info-row" t-if="equipment.ip_id">
                                                <div class="row">
                                                    <div class="col-sm-4 fw-semibold text-muted">IP:</div>
                                                    <div class="col-sm-8 fw-mono text-info" t-field="equipment.ip_id"/>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Información del Contrato -->
                        <div class="col-md-6 mb-4">
                            <div class="info-card animate__animated animate__fadeInRight animate__delay-1s">
                                <div class="info-card-header">
                                    <h5 class="fw-bold mb-0 text-success">
                                        <i class="fa fa-file-contract me-2"></i>
                                        Contrato
                                    </h5>
                                </div>
                                <div class="card-body p-0">
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-6 fw-semibold text-muted">Cotización:</div>
                                            <div class="col-6 fw-mono" t-field="equipment.secuencia"/>
                                        </div>
                                    </div>
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-6 fw-semibold text-muted">Inicio:</div>
                                            <div class="col-6" t-field="equipment.fecha_inicio_alquiler"/>
                                        </div>
                                    </div>
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-6 fw-semibold text-muted">Fin:</div>
                                            <div class="col-6" t-field="equipment.fecha_fin_alquiler"/>
                                        </div>
                                    </div>
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-6 fw-semibold text-muted">Duración:</div>
                                            <div class="col-6" t-field="equipment.duracion_alquiler_id.name"/>
                                        </div>
                                    </div>
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-6 fw-semibold text-muted">Estado:</div>
                                            <div class="col-6">
                                                <span t-field="equipment.estado_renovacion" 
                                                      t-attf-class="badge {{ equipment.estado_renovacion == 'vigente' and 'bg-success' or (equipment.estado_renovacion == 'por_vencer' and 'bg-warning' or 'bg-danger') }}"/>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Información de Costos -->
                        <div class="col-md-6 mb-4">
                            <div class="info-card animate__animated animate__fadeInRight animate__delay-2s">
                                <div class="info-card-header">
                                    <h5 class="fw-bold mb-0 text-info">
                                        <i class="fa fa-money-bill-wave me-2"></i>
                                        Costos
                                    </h5>
                                </div>
                                <div class="card-body p-0">
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-7 fw-semibold text-muted">Copia B/N:</div>
                                            <div class="col-5 text-end">
                                                <span class="fw-bold text-primary">S/ <span t-field="equipment.costo_copia_bn"/></span>
                                                <t t-if="equipment.precio_bn_incluye_igv">
                                                    <br/><small class="text-muted">(inc. IGV)</small>
                                                </t>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="info-row" t-if="equipment.tipo == 'color'">
                                        <div class="row">
                                            <div class="col-7 fw-semibold text-muted">Copia Color:</div>
                                            <div class="col-5 text-end">
                                                <span class="fw-bold text-danger">S/ <span t-field="equipment.costo_copia_color"/></span>
                                                <t t-if="equipment.precio_color_incluye_igv">
                                                    <br/><small class="text-muted">(inc. IGV)</small>
                                                </t>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-7 fw-semibold text-muted">Vol. mensual B/N:</div>
                                            <div class="col-5 text-end fw-bold" t-field="equipment.volumen_mensual_bn"/>
                                        </div>
                                    </div>
                                    <div class="info-row" t-if="equipment.tipo == 'color'">
                                        <div class="row">
                                            <div class="col-7 fw-semibold text-muted">Vol. mensual Color:</div>
                                            <div class="col-5 text-end fw-bold" t-field="equipment.volumen_mensual_color"/>
                                        </div>
                                    </div>
                                    <div class="info-row">
                                        <div class="row">
                                            <div class="col-7 fw-semibold text-muted">Facturación día:</div>
                                            <div class="col-5 text-end fw-bold" t-field="equipment.dia_facturacion"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Especificaciones Técnicas -->
                        <div class="col-12 mb-4" t-if="equipment.especificaciones_id">
                            <div class="info-card animate__animated animate__fadeInUp">
                                <div class="info-card-header">
                                    <h5 class="fw-bold mb-0 text-secondary">
                                        <i class="fa fa-cogs me-2"></i>
                                        Especificaciones Técnicas
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <div t-field="equipment.especificaciones_id" class="specifications-content"/>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <script>
            // Efectos adicionales
            document.addEventListener('DOMContentLoaded', function() {
                // Efecto hover en las tarjetas de información
                const infoRows = document.querySelectorAll('.info-row');
                infoRows.forEach(row =&gt; {
                    row.addEventListener('mouseenter', function() {
                        this.style.transform = 'translateX(5px)';
                        this.style.transition = 'transform 0.2s ease';
                    });
                    
                    row.addEventListener('mouseleave', function() {
                        this.style.transform = 'translateX(0)';
                    });
                });
                
                // Animación secuencial de las tarjetas de acción
                const actionCards = document.querySelectorAll('.action-card');
                actionCards.forEach((card, index) =&gt; {
                    setTimeout(() =&gt; {
                        card.style.opacity = '0';
                        card.style.transform = 'translateY(20px)';
                        card.style.transition = 'all 0.5s ease';
                        
                        setTimeout(() =&gt; {
                            card.style.opacity = '1';
                            card.style.transform = 'translateY(0)';
                        }, 100);
                    }, index * 100);
                });
            });
        </script>
    </t>
</template>
    
    <!-- Template para la creación pública de tickets desde QR -->
    <template id="portal_public_create_ticket" name="Create Ticket from QR">
        <t t-call="website.layout">
            <div class="container py-5">
                <div class="row">
                    <div class="col-lg-8 mx-auto">
                        <div class="card shadow">
                            <div class="card-header bg-primary text-white">
                                <h3 class="mb-0">Crear ticket de soporte</h3>
                            </div>
                            <div class="card-body">
                                <!-- Mensaje de éxito o error -->
                                <div t-if="success_message" class="alert alert-success">
                                    <span t-esc="success_message"/>
                                </div>
                                <div t-if="error_message" class="alert alert-danger">
                                    <span t-esc="error_message"/>
                                </div>
                                
                                <!-- Información del equipo -->
                                <div class="mb-4">
                                    <h5>Información del equipo</h5>
                                    <div class="row">
                                        <div class="col-md-4 text-center" t-if="equipment.imagen_id">
                                            <img class="img-fluid rounded" t-att-src="image_data_uri(equipment.imagen_id)" alt="Imagen de la Máquina"/>
                                        </div>
                                        <div t-att-class="equipment.imagen_id and 'col-md-8' or 'col-md-12'">
                                            <p><strong>Modelo:</strong> <span t-field="equipment.name.name"/></p>
                                            <p><strong>Serie:</strong> <span t-field="equipment.serie_id"/></p>
                                            <p><strong>Tipo:</strong> 
                                                <span t-if="equipment.tipo == 'monocroma'">Blanco y negro</span>
                                                <span t-else="">Color</span>
                                            </p>
                                        </div>
                                    </div>
                                </div>
                                
                                <!-- Formulario de ticket -->
                                <form method="post" t-if="not success_message">
                                    <div class="mb-3">
                                        <label for="name" class="form-label">Nombre completo</label>
                                        <input type="text" class="form-control" name="name" id="name" required="required"/>
                                    </div>
                                    <div class="mb-3">
                                        <label for="email" class="form-label">Correo electrónico</label>
                                        <input type="email" class="form-control" name="email" id="email" required="required"/>
                                    </div>
                                    <div class="mb-3">
                                        <label for="phone" class="form-label">Teléfono</label>
                                        <input type="tel" class="form-control" name="phone" id="phone"/>
                                    </div>
                                    <div class="mb-3">
                                        <label for="description" class="form-label">Descripción del problema</label>
                                        <textarea class="form-control" name="description" id="description" rows="5" required="required"></textarea>
                                    </div>
                                    <div class="text-end">
                                        <button type="submit" class="btn btn-primary">Enviar ticket</button>
                                    </div>
                                </form>
                                
                                <!-- Botones adicionales -->
                                <div class="mt-4 text-center">
                                    <a href="/" class="btn btn-secondary">Volver a la página principal</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
    
    <!-- Template para la página de contadores -->
    <template id="portal_my_copier_counters" name="My Equipment Counters">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            
            <!-- Incluir Chart.js y nuestro archivo JavaScript -->
            <t t-set="head">
                <link rel="stylesheet" type="text/css" href="/copier_company/static/src/css/counter_charts.css"/>
                <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
                <script type="text/javascript" src="/copier_company/static/src/js/counter_charts.js"></script>
            </t>
            
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Historial de Lecturas</t>
            </t>
            
            <div class="row mt-4">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-header">
                            <h4 class="mb-0">
                                <i class="fa fa-tachometer me-2"></i>Lecturas de la máquina 
                                <strong t-field="equipment.name.name"/> 
                                (Serie: <span t-field="equipment.serie_id"/>)
                            </h4>
                        </div>
                        <div class="card-body">
                            <t t-if="not counters">
                                <div class="alert alert-info text-center">
                                    <p>No hay registros de lecturas para este equipo</p>
                                </div>
                            </t>
                            <t t-if="counters">
                                <!-- Gráficos de consumo -->
                                <div class="mb-4">
                                    <h5>Gráficos de Consumo</h5>
                                    <ul class="nav nav-tabs" id="graphTabs" role="tablist">
                                        <li class="nav-item" role="presentation">
                                            <button class="nav-link active" id="monthly-tab" data-bs-toggle="tab" data-bs-target="#monthly" type="button" role="tab" aria-controls="monthly" aria-selected="true">Consumo Mensual</button>
                                        </li>
                                        <li class="nav-item" role="presentation">
                                            <button class="nav-link" id="yearly-tab" data-bs-toggle="tab" data-bs-target="#yearly" type="button" role="tab" aria-controls="yearly" aria-selected="false">Consumo Anual</button>
                                        </li>
                                        <li class="nav-item" role="presentation">
                                            <button class="nav-link" id="user-monthly-tab" data-bs-toggle="tab" data-bs-target="#user-monthly" type="button" role="tab" aria-controls="user-monthly" aria-selected="false">Copias Mensuales por Usuario</button>
                                        </li>
                                    </ul>
                                    <div class="tab-content p-3 border border-top-0 rounded-bottom" id="graphTabsContent">
                                        <div class="tab-pane fade show active" id="monthly" role="tabpanel" aria-labelledby="monthly-tab">
                                            <div class="chart-container">
                                                <canvas id="monthlyChart"></canvas>
                                            </div>
                                        </div>
                                        <div class="tab-pane fade" id="yearly" role="tabpanel" aria-labelledby="yearly-tab">
                                            <div class="chart-container">
                                                <canvas id="yearlyChart"></canvas>
                                            </div>
                                        </div>
                                        <div class="tab-pane fade" id="user-monthly" role="tabpanel" aria-labelledby="user-monthly-tab">
                                            <div class="chart-container">
                                                <canvas id="userMonthlyChart"></canvas>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="table-responsive">
                                    <table class="table table-striped">
                                        <thead>
                                            <tr>
                                                <th>Referencia</th>
                                                <th>Fecha</th>
                                                <th>Mes Facturación</th>
                                                <th>Anterior B/N</th>
                                                <th>Actual B/N</th>
                                                <th>Total B/N</th>
                                                <th t-if="equipment.tipo == 'color'">Anterior Color</th>
                                                <th t-if="equipment.tipo == 'color'">Actual Color</th>
                                                <th t-if="equipment.tipo == 'color'">Total Color</th>
                                                <th>Estado</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <tr t-foreach="counters" t-as="counter">
                                                <td>
                                                    <span t-field="counter.name"/>
                                                </td>
                                                <td>
                                                    <span t-field="counter.fecha"/>
                                                </td>
                                                <td>
                                                    <span t-field="counter.mes_facturacion"/>
                                                </td>
                                                <td class="text-end">
                                                    <span t-field="counter.contador_anterior_bn"/>
                                                </td>
                                                <td class="text-end">
                                                    <span t-field="counter.contador_actual_bn"/>
                                                </td>
                                                <td class="text-end">
                                                    <span t-field="counter.total_copias_bn"/>
                                                </td>
                                                <td t-if="equipment.tipo == 'color'" class="text-end">
                                                    <span t-field="counter.contador_anterior_color"/>
                                                </td>
                                                <td t-if="equipment.tipo == 'color'" class="text-end">
                                                    <span t-field="counter.contador_actual_color"/>
                                                </td>
                                                <td t-if="equipment.tipo == 'color'" class="text-end">
                                                    <span t-field="counter.total_copias_color"/>
                                                </td>
                                                <td>
                                                    <span t-field="counter.state" t-attf-class="badge rounded-pill bg-{{ counter.state == 'draft' and 'warning' or (counter.state == 'confirmed' and 'success' or (counter.state == 'invoiced' and 'info' or 'danger')) }}"/>
                                                </td>
                                            </tr>
                                        </tbody>
                                    </table>
                                </div>
                                
                                <!-- Resumen de facturación -->
                                <div class="mt-4">
                                    <h5>Resumen de Facturación</h5>
                                    <div class="row mb-4">
                                        <div class="col-md-6">
                                            <div class="card shadow-sm">
                                                <div class="card-header bg-light">
                                                    <h6 class="mb-0">Volumen Mensual Contratado</h6>
                                                </div>
                                                <div class="card-body">
                                                    <div class="row">
                                                        <div class="col-sm-6">
                                                            <p><strong>B/N:</strong> <span t-field="equipment.volumen_mensual_bn"/> copias</p>
                                                        </div>
                                                        <div class="col-sm-6" t-if="equipment.tipo == 'color'">
                                                            <p><strong>Color:</strong> <span t-field="equipment.volumen_mensual_color"/> copias</p>
                                                        </div>
                                                    </div>
                                                    <div class="row">
                                                        <div class="col-sm-6">
                                                            <p><strong>Precio B/N:</strong> <span t-field="equipment.costo_copia_bn" t-options="{'widget': 'monetary', 'display_currency': equipment.currency_id}"/></p>
                                                        </div>
                                                        <div class="col-sm-6" t-if="equipment.tipo == 'color'">
                                                            <p><strong>Precio Color:</strong> <span t-field="equipment.costo_copia_color" t-options="{'widget': 'monetary', 'display_currency': equipment.currency_id}"/></p>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="card shadow-sm">
                                                <div class="card-header bg-light">
                                                    <h6 class="mb-0">Consumo Actual</h6>
                                                </div>
                                                <div class="card-body">
                                                    <t t-if="counters and counters[0].state != 'draft'">
                                                        <div class="row">
                                                            <div class="col-sm-6">
                                                                <p><strong>Total B/N:</strong> <span t-field="counters[0].total_copias_bn"/> copias</p>
                                                                <p><strong>Exceso B/N:</strong> <span t-field="counters[0].exceso_bn"/> copias</p>
                                                            </div>
                                                            <div class="col-sm-6" t-if="equipment.tipo == 'color'">
                                                                <p><strong>Total Color:</strong> <span t-field="counters[0].total_copias_color"/> copias</p>
                                                                <p><strong>Exceso Color:</strong> <span t-field="counters[0].exceso_color"/> copias</p>
                                                            </div>
                                                        </div>
                                                    </t>
                                                    <div t-else="" class="alert alert-info">
                                                        No hay datos de consumo confirmados para el período actual.
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Detalle mensual por usuario (si está activado) -->
            <t t-if="counters and equipment and counters[0].informe_por_usuario">
                <div class="card mt-4">
                <div class="card-header bg-dark text-white">
                    <h5 class="mb-0">
                    <i class="fa fa-users me-2"></i>Detalle de Copias por Usuario
                    </h5>
                </div>
                <div class="card-body">
                    <t t-if="counters[0].usuario_detalle_ids">
                    <div class="table-responsive">
                        <table class="table table-sm table-striped">
                        <thead>
                            <tr>
                            <th>Usuario / Empresa</th>
                            <th>Total de Copias</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="counters[0].usuario_detalle_ids" t-as="line">
                            <td><t t-esc="line.usuario_id.name"/></td>
                            <td><t t-esc="line.cantidad_copias"/></td>
                            </tr>
                        </tbody>
                        </table>
                    </div>
                    </t>
                    <t t-else="">
                    <div class="alert alert-warning text-center">
                        No hay datos de copias por usuario en este período.
                    </div>
                    </t>
                </div>
                </div>
            </t>
            <!-- Gráfico por usuario -->
            <div class="mt-4">
                <h5>Gráfico de Copias por Usuario</h5>
                <div class="chart-container" style="height: 300px;">
                    <canvas id="userChart"></canvas>
                </div>
            </div>
    
            <!-- Botón para volver a la página de detalle del equipo -->
            <div class="text-center mt-4">
                <a t-att-href="'/my/copier/equipment/%s' % equipment.id" class="btn btn-secondary">
                    <i class="fa fa-arrow-left me-1"></i>Volver al Equipo
                </a>
            </div>
            
            <!-- Elemento con datos para los gráficos -->
            <div id="charts-data" 
                 t-att-data-chart-data="chart_data"
                 t-att-data-is-color="'true' if equipment.tipo == 'color' else 'false'"
                 style="display: none;"></div>
        </t>
    </template>
</odoo>