<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="pcloud_files_template" name="Descargas">
        <t t-call="website.layout">
            <div class="container-fluid">
                <div class="row mt-4 mb-4">
                    <div class="col-6 col-md-3">
                        <a href="/soporte/descargas?folder_id=0" class="btn btn-primary btn-block btn-md-inline">
                            <i class="fa fa-home"></i> Inicio
                        </a>
                        <t t-if="current_folder_id">
                            <a href="javascript:history.back()" class="btn btn-secondary btn-block btn-md-inline mt-2 mt-md-0">
                                <i class="fa fa-arrow-left"></i> Atrás
                            </a>
                        </t>
                    </div>
                    <div class="col-12 col-md-6 text-center mt-2 mt-md-0">
                        <form id="search-form" class="form-inline justify-content-center">
                            <input type="hidden" name="folder_id" t-att-value="current_folder_id"/>
                            <input type="text" id="search-input" class="form-control" placeholder="Buscar archivos..." t-att-value="request.params.get('search', '')"/>
                        </form>
                    </div>
                    <div class="col-6 col-md-3 text-right mt-2 mt-md-0">
                        <button id="list-view-btn" class="btn btn-outline-secondary" title="Vista de lista">
                            <img src="/copier_company/static/src/img/list-view-icon.png" alt="Lista" style="width: 24px; height: 24px;"/>
                        </button>
                        <button id="kanban-view-btn" class="btn btn-outline-secondary" title="Vista Kanban">
                            <img src="/copier_company/static/src/img/kanban-view-icon.png" alt="Kanban" style="width: 24px; height: 24px;"/>
                        </button>
                    </div>
                </div>
                
                <!-- Botones para gestión de archivos, visibles solo para usuarios del grupo específico -->
                <t t-if="request.env.user.has_group('base.group_system')">
                    <div class="row mb-3">
                        <div class="col-12 text-right">
                            <button id="create-file-btn" class="btn btn-success">Crear Archivo</button>
                            <button id="create-folder-btn" class="btn btn-info">Crear Carpeta</button>
                            <button id="upload-file-btn" class="btn btn-primary">Subir Archivo</button>
                        </div>
                    </div>
                </t>

                <div class="row">
                    <div class="col-12 table-responsive" id="list-view">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Nombre</th>
                                    <th>Tamaño</th>
                                    <th id="modified-header">Fecha de Modificación <i id="sort-icon" class="fa"></i></th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="files-list">
                                <t t-foreach="contents" t-as="item">
                                    <tr>
                                        <td>
                                            <img t-att-src="'/copier_company/static/src/img/' + item['icon']" alt="icon" style="width: 24px; height: 24px;"/>
                                            <t t-if="item['isfolder']">
                                                <a t-att-href="'/soporte/descargas?folder_id=%d&amp;view_mode=%s' % (item['id'], view_mode)">
                                                    <t t-esc="item['name']"/>
                                                </a>
                                            </t>
                                            <t t-else="">
                                                <t t-esc="item['name']"/>
                                            </t>
                                        </td>
                                        <td>
                                            <t t-esc="item['size']"/>
                                        </td>
                                        <td>
                                            <t t-esc="item['modified']"/>
                                        </td>
                                        <td>
                                            <t t-if="not item['isfolder']">
                                                <a t-att-href="'/soporte/descarga?file_id=%d' % item['id']" class="btn btn-outline-primary btn-md-inline">
                                                    Descargar
                                                </a>
                                            </t>
                                            <t t-if="request.env.user.has_group('base.group_system')">
                                                <button class="btn btn-danger btn-md-inline" onclick="deleteFile(<t t-esc='item.id' />)">
                                                    <i class="fa fa-trash"></i>
                                                </button>
                                                <button class="btn btn-warning btn-md-inline" onclick="renameFile(<t t-esc='item.id' />)">
                                                    <i class="fa fa-pencil"></i>
                                                </button>
                                            </t>
                                        </td>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                    </div>
                    <div class="col-12 d-none" id="kanban-view">
                        <div class="row">
                            <t t-foreach="contents" t-as="item">
                                <div class="col-6 col-md-4 col-lg-3 mb-4">
                                    <div class="card">
                                        <img class="card-img-top" t-att-src="'/copier_company/static/src/img/' + item['icon']" alt="icon" style="width: 48px; height: 48px; margin: auto;"/>
                                        <div class="card-body">
                                            <h5 class="card-title">
                                                <t t-esc="item['name']"/>
                                            </h5>
                                            <p class="card-text">
                                                <strong>Modificado:</strong> <t t-esc="item['modified']"/>
                                            </p>
                                            <t t-if="item['isfolder']">
                                                <a t-att-href="'/soporte/descargas?folder_id=%d&amp;view_mode=%s' % (item['id'], view_mode)" class="btn btn-primary">Abrir</a>
                                            </t>
                                            <t t-if="not item['isfolder']">
                                                <a t-att-href="'/soporte/descarga?file_id=%d' % item['id']" class="btn btn-outline-primary">Descargar</a>
                                            </t>
                                            <t t-if="request.env.user.has_group('base.group_system')">
                                                <button class="btn btn-danger" onclick="deleteFile(<t t-esc='item.id' />)">
                                                    <i class="fa fa-trash"></i>
                                                </button>
                                                <button class="btn btn-warning" onclick="renameFile(<t t-esc='item.id' />)">
                                                    <i class="fa fa-pencil"></i>
                                                </button>
                                            </t>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
            <script type="text/javascript">
                document.addEventListener('DOMContentLoaded', function() {
                    var searchInput = document.getElementById('search-input');
                    var filesList = document.getElementById('files-list');
                    var listViewBtn = document.getElementById('list-view-btn');
                    var kanbanViewBtn = document.getElementById('kanban-view-btn');
                    var listView = document.getElementById('list-view');
                    var kanbanView = document.getElementById('kanban-view');
                    var viewMode = localStorage.getItem('viewMode') || 'list';
                    var modifiedHeader = document.getElementById('modified-header');
                    var sortIcon = document.getElementById('sort-icon');
                    var ascending = true;

                    function setViewMode(mode) {
                        if (mode === 'list') {
                            listView.classList.remove('d-none');
                            kanbanView.classList.add('d-none');
                            listViewBtn.classList.add('active');
                            kanbanViewBtn.classList.remove('active');
                        } else {
                            listView.classList.add('d-none');
                            kanbanView.classList.remove('d-none');
                            listViewBtn.classList.remove('active');
                            kanbanViewBtn.classList.add('active');
                        }
                        localStorage.setItem('viewMode', mode);
                    }

                    listViewBtn.addEventListener('click', function() {
                        setViewMode('list');
                    });

                    kanbanViewBtn.addEventListener('click', function() {
                        setViewMode('kanban');
                    });

                    searchInput.addEventListener('input', function() {
                        var searchTerm = searchInput.value.toLowerCase();
                        var rows = filesList.getElementsByTagName('tr');
                        for (var i = 0; i &lt; rows.length; i++) {
                            var row = rows[i];
                            var fileName = row.getElementsByTagName('td')[0].innerText.toLowerCase();
                            if (fileName.includes(searchTerm)) {
                                row.style.display = '';
                            } else {
                                row.style.display = 'none';
                            }
                        }

                        var cards = kanbanView.getElementsByClassName('card');
                        for (var i = 0; i &lt; cards.length; i++) {
                            var card = cards[i];
                            var fileName = card.getElementsByClassName('card-title')[0].innerText.toLowerCase();
                            if (fileName.includes(searchTerm)) {
                                card.style.display = '';
                            } else {
                                card.style.display = 'none';
                            }
                        }
                    });

                    modifiedHeader.addEventListener('click', function() {
                        var rows = Array.from(filesList.getElementsByTagName('tr'));
                        rows.sort(function(a, b) {
                            var dateA = new Date(a.getElementsByTagName('td')[2].innerText);
                            var dateB = new Date(b.getElementsByTagName('td')[2].innerText);
                            return ascending ? dateA - dateB : dateB - dateA;
                        });

                        ascending = !ascending;
                        sortIcon.className = ascending ? 'fa fa-arrow-up' : 'fa fa-arrow-down';

                        rows.forEach(function(row) {
                            filesList.appendChild(row);
                        });
                    });

                    // Funciones de gestión de archivos
                    function deleteFile(fileId) {
                        var confirmed = confirm("¿Está seguro de que desea eliminar este archivo?");
                        if (confirmed) {
                            // Lógica para eliminar el archivo
                            alert("Archivo eliminado.");
                        }
                    }

                    function renameFile(fileId) {
                        var newFileName = prompt("Ingrese el nuevo nombre del archivo:");
                        if (newFileName) {
                            // Lógica para renombrar el archivo
                            alert("Archivo renombrado a '" + newFileName + "'.");
                        }
                    }

                    setViewMode(viewMode);
                });
            </script>
        </t>
    </template>
</odoo>
