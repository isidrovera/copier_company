<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <template id="pcloud_files_template" name="Pcloud Files">
        <t t-call="website.layout">
            <div class="container">
                <div class="row mt-4 mb-4">
                    <div class="col-md-6">
                        <a href="/pcloud/files?folder_id=0" class="btn btn-primary">
                            <i class="fa fa-home"></i> Inicio
                        </a>
                        <t t-if="current_folder_id">
                            <a href="javascript:history.back()" class="btn btn-secondary">
                                <i class="fa fa-arrow-left"></i> Atrás
                            </a>
                        </t>
                    </div>
                    <div class="col-md-6">
                        <form id="search-form" class="form-inline float-right">
                            <input type="hidden" name="folder_id" t-att-value="current_folder_id"/>
                            <input type="text" id="search-input" class="form-control mr-sm-2" placeholder="Buscar archivos..." t-att-value="request.params.get('search', '')"/>
                            <button type="button" class="btn btn-outline-success">Buscar</button>
                        </form>
                    </div>
                </div>
                <div class="row">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody id="files-list">
                            <t t-foreach="contents" t-as="item">
                                <tr>
                                    <td>
                                        <t t-if="item['isfolder']">
                                            <i class="fa fa-folder"></i>
                                            <a t-att-href="'/pcloud/files?folder_id=%d' % item['id']">
                                                <t t-esc="item['name']"/>
                                            </a>
                                        </t>
                                        <t t-if="not item['isfolder']">
                                            <i class="fa fa-file"></i>
                                            <t t-esc="item['name']"/>
                                        </t>
                                    </td>
                                    <td>
                                        <t t-if="not item['isfolder']">
                                            <a t-att-href="'/pcloud/download?file_id=%d' % item['id']" class="btn btn-outline-primary">
                                                Descargar
                                            </a>
                                        </t>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </div>
            </div>
            <script type="text/javascript">
                document.addEventListener('DOMContentLoaded', function() {
                    var searchInput = document.getElementById('search-input');
                    var filesList = document.getElementById('files-list');

                    searchInput.addEventListener('input', function() {
                        var searchTerm = searchInput.value.toLowerCase();
                        var rows = filesList.getElementsByTagName('tr');

                        for (var i = 0; i < rows.length; i++) {
                            var row = rows[i];
                            var fileName = row.getElementsByTagName('td')[0].innerText.toLowerCase();
                            if (fileName.includes(searchTerm)) {
                                row.style.display = '';
                            } else {
                                row.style.display = 'none';
                            }
                        }
                    });
                });
            </script>
        </t>
    </template>
</odoo>
