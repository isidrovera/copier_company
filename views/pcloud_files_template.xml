<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="pcloud_files_template" name="Pcloud Files">
        <t t-call="website.layout">
            <div class="container">
                <div class="row mt-4 mb-4">
                    <div class="col-md-4">
                        <a href="/pcloud/files?folder_id=0" class="btn btn-primary">
                            <i class="fa fa-home"></i> Inicio
                        </a>
                        <t t-if="current_folder_id">
                            <a href="javascript:history.back()" class="btn btn-secondary">
                                <i class="fa fa-arrow-left"></i> Atrás
                            </a>
                        </t>
                    </div>
                    <div class="col-md-4 text-center">
                        <form id="search-form" class="form-inline justify-content-center">
                            <input type="hidden" name="folder_id" t-att-value="current_folder_id"/>
                            <input type="text" id="search-input" class="form-control mr-sm-2" placeholder="Buscar archivos..." t-att-value="request.params.get('search', '')"/>
                        </form>
                    </div>
                    <div class="col-md-4 text-right">
                        <button id="list-view-btn" class="btn btn-outline-secondary" title="Vista de lista">
                            <img src="/copier_company/static/src/img/list-view-icon.png" alt="Lista" style="width: 24px; height: 24px;"/>
                        </button>
                        <button id="kanban-view-btn" class="btn btn-outline-secondary" title="Vista Kanban">
                            <img src="/copier_company/static/src/img/kanban-view-icon.png" alt="Kanban" style="width: 24px; height: 24px;"/>
                        </button>
                    </div>
                </div>
                <div class="row" id="files-container">
                    <table id="list-view" class="table table-striped" style="display: none;">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Tamaño</th>
                                <th>Fecha de Modificación</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody id="files-list">
                            <t t-foreach="contents" t-as="item">
                                <tr>
                                    <td>
                                        <img t-att-src="'/copier_company/static/src/img/' + item['icon']" alt="icon" style="width: 24px; height: 24px;"/>
                                        <t t-if="item['isfolder']">
                                            <a t-att-href="'/pcloud/files?folder_id=%d&view_mode=' + viewMode" t-att-data-view-mode="viewMode">
                                                <t t-esc="item['name']"/>
                                            </a>
                                        </t>
                                        <t t-else="">
                                            <t t-esc="item['name']"/>
                                        </t>
                                    </td>
                                    <td>
                                        <t t-esc="item['size']"/>
                                    </td>
                                    <td>
                                        <t t-esc="item['modified']"/>
                                    </td>
                                    <td>
                                        <t t-if="not item['isfolder']">
                                            <a t-att-href="'/pcloud/download?file_id=%d' % item['id']" class="btn btn-outline-primary">
                                                Descargar
                                            </a>
                                        </t>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                    <div id="kanban-view" class="d-flex flex-wrap" style="display: none;">
                        <t t-foreach="contents" t-as="item">
                            <div class="card m-2" style="width: 18rem;">
                                <img t-att-src="'/copier_company/static/src/img/' + item['icon']" class="card-img-top" alt="icon" style="width: 48px; height: 48px; margin: auto;"/>
                                <div class="card-body">
                                    <h5 class="card-title"><t t-esc="item['name']"/></h5>
                                    <p class="card-text">
                                        <strong>Tamaño:</strong> <t t-esc="item['size']"/><br/>
                                        <strong>Modificado:</strong> <t t-esc="item['modified']"/>
                                    </p>
                                    <t t-if="item['isfolder']">
                                        <a t-att-href="'/pcloud/files?folder_id=%d&view_mode=' + viewMode" t-att-data-view-mode="viewMode" class="btn btn-primary">Abrir</a>
                                    </t>
                                    <t t-else="">
                                        <a t-att-href="'/pcloud/download?file_id=%d' % item['id']" class="btn btn-outline-primary">Descargar</a>
                                    </t>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>
            </div>
            <script type="text/javascript">
                document.addEventListener('DOMContentLoaded', function() {
                    var searchInput = document.getElementById('search-input');
                    var filesList = document.getElementById('files-list');
                    var listViewBtn = document.getElementById('list-view-btn');
                    var kanbanViewBtn = document.getElementById('kanban-view-btn');
                    var listView = document.getElementById('list-view');
                    var kanbanView = document.getElementById('kanban-view');
                    var viewMode = new URLSearchParams(window.location.search).get('view_mode') || 'list';

                    function setViewMode(mode) {
                        if (mode === 'kanban') {
                            listView.style.display = 'none';
                            kanbanView.style.display = 'flex';
                        } else {
                            listView.style.display = 'table';
                            kanbanView.style.display = 'none';
                        }
                    }

                    listViewBtn.addEventListener('click', function() {
                        setViewMode('list');
                    });

                    kanbanViewBtn.addEventListener('click', function() {
                        setViewMode('kanban');
                    });

                    searchInput.addEventListener('input', function() {
                        var searchTerm = searchInput.value.toLowerCase();
                        var rows = filesList.getElementsByTagName('tr');
                        for (var i = 0; i &lt; rows.length; i++) {
                            var row = rows[i];
                            var fileName = row.getElementsByTagName('td')[0].innerText.toLowerCase();
                            if (fileName.includes(searchTerm)) {
                                row.style.display = '';
                            } else {
                                row.style.display = 'none';
                            }
                        }
                    });

                    setViewMode(viewMode);
                });
            </script>
        </t>
    </template>
</odoo>
