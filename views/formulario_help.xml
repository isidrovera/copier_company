<odoo>
    <template id="ticket_form_inherit" inherit_id="website_helpdesk.team_form_1" name="Helpdesk Ticket Form Inherit">
        <xpath expr="//input[@id='helpdesk3']/parent::div" position="replace">
            <div class="mb-0 py-2 s_website_form_field col-12" data-type="many2one" data-name="Field">
                <div class="row mb-3">
                    <label for="product_id" class="col-sm-3 col-form-label">Serie</label>
                    <div class="col-sm-9">
                        <select name="product_id" class="form-control" required="required">
                            <option value="">Seleccione una serie...</option>
                            <t t-foreach="products" t-as="product">
                                <option t-att-value="product.id">
                                    <t t-esc="product.display_name"/>
                                </option>
                            </t>
                        </select>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
</odoo>

