<?xml version="1.0" encoding="UTF-8"?>
<odoo>
 <!-- Kanban View -->
<record id="view_copier_counter_kanban" model="ir.ui.view">
  <field name="name">copier.counter.kanban</field>
  <field name="model">copier.counter</field>
  <field name="arch" type="xml">
    <kanban class="o_kanban_mobile"
            sample="1"
            default_group_by="state"
            create="1" quick_create="0">

      <!-- Declarar campos usados -->
      <field name="name"/>
      <field name="cliente_id"/>
      <field name="serie"/>
      <field name="fecha_facturacion"/>
      <field name="mes_facturacion"/>
      <field name="state"/>
      <field name="total"/>
      <field name="currency_id"/>

      <progressbar field="state"
                   colors='{
                     "draft": "info",
                     "confirmed": "success",
                     "invoiced": "warning",
                     "cancelled": "danger"
                   }'/>

      <templates>
        <t t-name="card">
          <div class="o_kanban_record o_kanban_global_click p-0">
            <div class="oe_kanban_details p-3">
              <!-- Header -->
              <div class="o_kanban_record_top mb-2 d-flex justify-content-between align-items-start">
                <div class="o_kanban_record_headings">
                  <strong class="o_kanban_record_title text-primary">
                    <field name="name"/>
                  </strong>
                </div>
                <div class="text-end">
                  <field name="total" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                </div>
              </div>

              <!-- Datos principales -->
              <div class="row mb-2">
                <div class="col-6">
                  <strong>Cliente:</strong> <field name="cliente_id"/>
                </div>
                <div class="col-6">
                  <strong>Serie:</strong> <field name="serie"/>
                </div>
              </div>

              <div class="row mb-2">
                <div class="col-6">
                  <field name="mes_facturacion"/>
                </div>
                <div class="col-6 text-end">
                  <!-- vacío a propósito -->
                </div>
              </div>

              <!-- Footer -->
              <div class="o_kanban_record_bottom d-flex justify-content-between align-items-center">
                <div>
                  <t t-set="estado" t-value="record.state.raw_value or ''"/>
                  <span class="badge rounded-pill"
                        t-att-class="{
                          'bg-success': estado == 'confirmed',
                          'bg-info':    estado == 'draft',
                          'bg-warning': estado == 'invoiced',
                          'bg-danger':  estado == 'cancelled'
                        }">
                    <field name="state"/>
                  </span>
                </div>
                <div class="text-muted">
                  <field name="fecha_facturacion" widget="date"/>
                </div>
              </div>

            </div>
          </div>
        </t>
      </templates>
    </kanban>
  </field>
</record>

    <!-- Tree/List View -->
     <record id="view_copier_counter_tree" model="ir.ui.view">
        <field name="name">copier.counter.tree</field>
        <field name="model">copier.counter</field>
        <field name="arch" type="xml">
            <list decoration-info="state == 'draft'"
                decoration-success="state == 'confirmed'"
                decoration-warning="state == 'invoiced'"
                decoration-danger="state == 'cancelled'"
                default_order="fecha desc, id desc"
                sample="1">

                <!--<field name="name" widget="many2one_avatar_user" />-->
                <field name="cliente_id" optional="hide" />
                <field name="maquina_id" />
                <field name="serie" />
                <field name="fecha" />
                <field name="mes_facturacion" optional="hide" />
                <field name="contador_actual_bn" sum="Total B/N" optional="hide"/>
                <field name="total_copias_bn" sum="Total Copias B/N" />
                <field name="copias_facturables_bn" sum="Total Facturable B/N" optional="hide"/>
                <field name="contador_actual_color" sum="Total Color" optional="hide"/>
                <field name="total_copias_color" sum="Total Copias Color" />
                <field name="copias_facturables_color" sum="Total Facturable Color" />
                
                <field name="subtotal" sum="Subtotal" widget="monetary" optional="hide"/>
                <field name="total" sum="Total" widget="monetary" />
                <field name="currency_id" optional="hide"/>
                <field name="state" widget="badge"
                    decoration-info="state == 'draft'"
                    decoration-success="state == 'confirmed'"
                    decoration-warning="state == 'invoiced'"
                    decoration-danger="state == 'cancelled'" />
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_copier_counter_form" model="ir.ui.view">
    <field name="name">copier.counter.form</field>
    <field name="model">copier.counter</field>
    <field name="arch" type="xml">
        <form>
            <header>
                <button name="action_confirm"
                    string="Confirmar"
                    type="object"
                    class="btn btn-primary"
                    invisible="state != 'draft'" />
                <button name="action_draft"
                    string="Volver a Borrador"
                    type="object"
                    class="btn btn-secondary"
                    invisible="state != 'confirmed'" />
                <button name="action_cancel"
                    string="Cancelar"
                    type="object"
                    class="btn btn-outline-danger"
                    invisible="state not in ('draft', 'confirmed')" />
                <button string="📄 Reporte" 
                    name="action_print_report" 
                    type="object"
                    class="btn btn-info" />
                <button name="action_create_invoice" 
                    type="object" 
                    string="💰 Crear Factura"
                    class="btn btn-success"
                    invisible="state != 'confirmed'"/>
                
                    <button name="action_send_counter_email"
                        type="object"
                        string="Enviar por correo"
                        class="oe_highlight"
                        
                />

                <field name="state" widget="statusbar"
                    options="{'clickable': '1'}" />
            </header>
            <sheet>
                <div class="oe_title">
                    <h1>
                        <field name="name" readonly="1" class="text-primary fw-bold" />
                    </h1>
                </div>
                
                <!-- Información Principal -->
                <div class="row">
                    <div class="col-lg-6">
                        <group string="📋 Información General" class="bg-light p-3 rounded">
                            <field name="maquina_id" readonly="state != 'draft'" />
                            <field name="cliente_id" />
                            <field name="serie" />
                            <field name="fecha" readonly="state != 'draft'" />
                            <field name="fecha_facturacion" readonly="state != 'draft'" />
                            <field name="mes_facturacion" />
                            <field name="fecha_emision_factura" 
       help="Deja vacío para usar la fecha de hoy al facturar"
       readonly="state == 'invoiced'"/>
                        </group>
                    </div>
                    <div class="col-lg-6">
                        <group string="👥 Gestión de Usuarios" class="bg-info bg-opacity-10 p-3 rounded">
                            <field name="informe_por_usuario" widget="boolean_toggle"/>
                            <button name="cargar_usuarios_asociados" 
                                    string="Cargar Usuarios Asociados"
                                    type="object" 
                                    class="btn btn-primary"
                                    invisible="not informe_por_usuario or state != 'draft'"/>
                        </group>
                    </div>
                </div>

                <field name="currency_id" invisible="1" />

                <!-- Detalle de Usuarios - Solo visible cuando informe_por_usuario está activado -->
                <div class="mt-3">
    <field name="usuario_detalle_ids" invisible="not informe_por_usuario">
        <list editable="bottom" class="table-striped">

            <!-- Usuario -->
            <field name="usuario_id" readonly="1"/>

            <!-- Copias B/N -->
            <field name="cantidad_bn"
                   string="B/N"
                   sum="Total B/N"
                   class="text-end"/>

            <!-- Copias Color -->
            <field name="cantidad_color"
                   string="Color"
                   sum="Total Color"
                   class="text-end"
                   invisible="tipo_maquina == 'monocroma'"/>

            <!-- Total -->
            <field name="total_copias"
                   string="Total"
                   sum="Gran Total"
                   readonly="1"
                   class="text-end"/>

        </list>
    </field>
</div>



                <notebook class="mt-4">
                    <page string="🖤 Contadores B/N" name="contadores_bn">
                        <div class="card border-dark">
                            <div class="card-header bg-dark text-white">
                                <h5 class="mb-0">Contadores Blanco y Negro</h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-6">
                                        <group string="Contadores">
                                            <field name="contador_anterior_bn" class="form-control-lg"/>
                                            <field name="contador_actual_bn" 
                                                   readonly="state != 'draft'" 
                                                   class="form-control-lg"/>
                                            <field name="total_copias_bn" 
                                                   class="fw-bold text-primary form-control-lg"/>
                                        </group>
                                    </div>
                                    <div class="col-md-6">
                                        <group string="Facturación">
                                            <field name="exceso_bn" class="text-warning"/>
                                            <field name="copias_facturables_bn" class="text-success"/>
                                            <field name="precio_bn_sin_igv" widget="monetary" class="text-info"/>
                                        </group>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </page>
                    
                    <page string="🌈 Contadores Color" name="contadores_color">
                        <div class="card border-primary">
                            <div class="card-header bg-primary text-white">
                                <h5 class="mb-0">Contadores a Color</h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-6">
                                        <group string="Contadores">
                                            <field name="contador_anterior_color" class="form-control-lg"/>
                                            <field name="contador_actual_color" 
                                                   readonly="state != 'draft'" 
                                                   class="form-control-lg"/>
                                            <field name="total_copias_color" 
                                                   class="fw-bold text-primary form-control-lg"/>
                                        </group>
                                    </div>
                                    <div class="col-md-6">
                                        <group string="Facturación">
                                            <field name="exceso_color" class="text-warning"/>
                                            <field name="copias_facturables_color" class="text-success"/>
                                            <field name="precio_color_sin_igv" widget="monetary" class="text-info"/>
                                        </group>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </page>
                    
                    <page string="💰 Totales" name="totales">
                        <div class="card border-success">
                            <div class="card-header bg-success text-white">
                                <h5 class="mb-0">Resumen Financiero</h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-6">
                                        <group class="oe_subtotal_footer bg-light p-3 rounded">
                                            <field name="subtotal" 
                                                   widget="monetary" 
                                                   class="fs-5 text-primary"/>
                                            <field name="igv" 
                                                   widget="monetary" 
                                                   class="fs-5 text-info"/>
                                            <field name="total" 
                                                   class="oe_subtotal_footer_separator fw-bold fs-4 text-success"
                                                   widget="monetary" />
                                        </group>
                                    </div>
                                    <div class="col-md-6">
                                        <separator string="📊 Información de Facturación" class="text-muted"/>
                                        <group col="1" class="mt-3">
                                            <field name="producto_facturable_id" 
                                                   readonly="1" 
                                                   class="fw-semibold"/>
                                            <field name="precio_producto" 
                                                   readonly="1" 
                                                   widget="monetary"
                                                   class="text-success"/>
                                        </group>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </page>
                </notebook>
            </sheet>
            <chatter />
        </form>
    </field>
</record>
    <!-- Search View -->
<record id="copier_counter_view_search" model="ir.ui.view">
        <field name="name">copier.counter.view.search</field>
        <field name="model">copier.counter</field>
        <field name="arch" type="xml">
            <search>
                <!-- Campos de búsqueda -->
                <field name="name" string="Referencia"/>
                <field name="cliente_id" string="Cliente"/>
                <field name="maquina_id" string="Máquina"/>
                <field name="serie" string="Serie"/>
                
                <separator/>
                
                <!-- Filtros por Estado -->
                <filter name="draft" string="Borrador"
                        domain="[('state', '=', 'draft')]"/>
                <filter name="confirmed" string="Confirmado"
                        domain="[('state', '=', 'confirmed')]"/>
                <filter name="invoiced" string="Facturado"
                        domain="[('state', '=', 'invoiced')]"/>
                <filter name="cancelled" string="Cancelado"
                        domain="[('state', '=', 'cancelled')]"/>
                
                <separator/>
                
                <!-- Filtros por Copias -->
                <filter name="con_exceso_bn" string="Con Exceso B/N"
                        domain="[('exceso_bn', '&gt;', 0)]"/>
                <filter name="con_exceso_color" string="Con Exceso Color"
                        domain="[('exceso_color', '&gt;', 0)]"/>
                
                <separator/>
                
                <!-- Filtros por Monto -->
                <filter name="montos_altos" string="Montos Altos (&gt; 1000)"
                        domain="[('total', '&gt;', 1000)]"/>
                <filter name="pendiente_facturar" string="Pendiente de Facturar"
                        domain="[('state', '=', 'confirmed')]"/>
                
                <separator/>
                
                <!-- Agrupaciones -->
                <group name="groupby">
                    <filter name="group_by_cliente" string="Cliente"
                            context="{'group_by': 'cliente_id'}"/>
                    <filter name="group_by_maquina" string="Máquina"
                            context="{'group_by': 'maquina_id'}"/>
                    <filter name="group_by_estado" string="Estado"
                            context="{'group_by': 'state'}"/>
                    <filter name="group_by_mes" string="Mes de Facturación"
                            context="{'group_by': 'mes_facturacion'}"/>
                    <filter name="group_by_fecha" string="Fecha de Lectura"
                            context="{'group_by': 'fecha:month'}"/>
                    <filter name="group_by_currency" string="Moneda"
                            context="{'group_by': 'currency_id'}"/>
                </group>
            </search>
        </field>
    </record>


</odoo>