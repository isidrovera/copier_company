<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================== -->
    <!-- VISTA LIST (antes tree) -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_list" model="ir.ui.view">
        <field name="name">copier.service.request.list</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <list string="Solicitudes de Servicio" 
                  multi_edit="1"
                  sample="1"
                  decoration-danger="prioridad == '3'"
                  decoration-warning="prioridad == '2'"
                  decoration-success="estado == 'completado'"
                  decoration-muted="estado == 'cancelado'">
                
                <field name="name" string="Número"/>
                <field name="create_date" string="Fecha Solicitud" optional="show"/>
                <field name="cliente_id" string="Cliente"/>
                <field name="modelo_maquina" string="Modelo" optional="show"/>
                <field name="serie_maquina" string="Serie" optional="hide"/>
                <field name="ubicacion" string="Ubicación" optional="show"/>
                <field name="tipo_problema_id" string="Tipo" optional="show"/>
                
                <field name="prioridad" 
                       string="Prioridad" 
                       widget="priority"
                       optional="show"/>
                
                <field name="estado" 
                       string="Estado"
                       widget="badge"
                       decoration-success="estado == 'completado'"
                       decoration-warning="estado in ('en_ruta', 'en_sitio')"
                       decoration-info="estado == 'asignado'"
                       decoration-danger="estado == 'pausado'"/>
                
                <field name="tecnico_id" string="Técnico" optional="show"/>
                <field name="fecha_programada" string="Programado" optional="show"/>
                <field name="requiere_repuestos"
                       string="Repuestos"
                       widget="boolean"
                       optional="show"/>
                <field name="sale_order_id"
                       string="Pedido"
                       optional="show"/>
                <field name="sale_order_state"
                       string="Estado pedido"
                       widget="badge"
                       optional="hide"
                       decoration-info="sale_order_state in ('draft', 'sent')"
                       decoration-success="sale_order_state == 'sale'"
                       decoration-muted="sale_order_state == 'cancel'"/>
                
                <field name="sla_limite_1" 
                       string="Límite SLA (hrs)" 
                       optional="hide"/>
                
                <field name="origen_solicitud" string="Origen" optional="hide"/>
                <field name="calificacion" string="Calificación" optional="hide" widget="priority"/>
                
                <field name="activity_ids" widget="list_activity"/>
                
                <field name="color" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- ========================================== -->
    <!-- VISTA FORM -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_form" model="ir.ui.view">
        <field name="name">copier.service.request.form</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <form string="Solicitud de Servicio">
                <header>
                    <button name="action_asignar_tecnico" 
                            type="object" 
                            string="Asignar Técnico"
                            icon="fa-user-plus"
                            invisible="estado != 'nuevo'"
                            class="btn-primary"/>
                    
                    <button name="action_confirmar_visita" 
                            type="object" 
                            string="Confirmar Visita"
                            invisible="estado != 'asignado'"
                            icon="fa-calendar-check-o"
                            class="btn-primary"/>
                    
                    <button name="action_iniciar_ruta" 
                            type="object" 
                            string="Iniciar Ruta"
                            invisible="estado != 'confirmado'"
                            icon="fa-car"
                            class="btn-warning"/>
                    
                    <button name="action_iniciar_servicio" 
                            type="object" 
                            string="Iniciar Servicio"
                            invisible="estado not in ('confirmado', 'en_ruta')"
                            icon="fa-play"
                            class="btn-success"/>
                    
                    <button name="action_pausar_servicio" 
                            type="object" 
                            string="Pausar"
                            invisible="estado != 'en_sitio'"
                            icon="fa-pause"/>

                    <button name="action_reanudar_servicio"
                            type="object"
                            string="Reanudar"
                            invisible="estado != 'pausado'"
                            icon="fa-play-circle"
                            class="btn-primary"/>
                    
                    <button name="action_completar_servicio" 
                            type="object" 
                            string="Completar Servicio"
                            invisible="estado != 'en_sitio'"
                            icon="fa-check"
                            class="btn-success"/>

                    <button name="action_create_parts_sale_order"
                            type="object"
                            string="Agregar Productos"
                            icon="fa-shopping-cart"
                            invisible="not requiere_repuestos or sale_order_id or estado not in ('en_sitio', 'pausado')"
                            class="btn-primary"/>

                    <button name="action_open_parts_sale_order"
                            type="object"
                            string="Abrir Pedido"
                            icon="fa-external-link"
                            invisible="not sale_order_id"/>
                    
                    <button name="action_cancelar_servicio" 
                            type="object" 
                            string="Cancelar"
                            invisible="estado in ('completado', 'cancelado')"
                            icon="fa-times"/>
                    <button
                        name="action_print_service_report"
                        type="object"
                        string="Imprimir Reporte"
                        icon="fa-print"
                        class="btn-primary"
                        invisible="estado != 'completado'"
                    />
                    
                    <field name="estado" widget="statusbar" 
                           statusbar_visible="nuevo,asignado,confirmado,en_ruta,en_sitio,pausado,completado,cancelado"
                           options="{'clickable': 0}"/>
                </header>

                <sheet>
                    <!-- Alertas y Ribbons -->
                    <div class="ribbon ribbon-top-right" invisible="prioridad != '3'">
                        <span class="bg-danger">CRÍTICA</span>
                    </div>

                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_whatsapp_notifications"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-whatsapp"
                                invisible="whatsapp_notifications_count == 0">
                            <field name="whatsapp_notifications_count"
                                   widget="statinfo"
                                   string="WhatsApp"/>
                        </button>
                    </div>

                    <div class="alert alert-warning d-flex align-items-center gap-2" role="alert"
                         invisible="not requiere_repuestos or sale_order_id or estado not in ('en_sitio', 'pausado')">
                        <i class="fa fa-exclamation-triangle fa-lg"/>
                        <div>
                            <strong>Pedido pendiente:</strong>
                            indicó que se requieren repuestos, pero todavía no se creó la cotización.
                        </div>
                    </div>

                    <div class="alert alert-info d-flex align-items-center gap-2" role="alert"
                         invisible="not sale_order_id">
                        <i class="fa fa-shopping-cart fa-lg"/>
                        <div>
                            Pedido vinculado:
                            <field name="sale_order_id" readonly="1" class="fw-bold"/>
                            <span class="ms-2">Estado:</span>
                            <field name="sale_order_state" widget="badge" readonly="1"/>
                        </div>
                    </div>

                    <!-- Título -->
                    <div class="oe_title mb-3">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                        <h2>
                            <field name="tipo_problema_id" 
                                   placeholder="Seleccione tipo de problema..."
                                   readonly="estado in ('completado', 'cancelado')"/>
                        </h2>
                        <div class="text-muted mt-1">
                            <field name="cliente_id" readonly="1"/>
                            <span class="mx-2">•</span>
                            <field name="modelo_maquina" readonly="1"/>
                            <span class="mx-2">•</span>
                            Serie <field name="serie_maquina" readonly="1"/>
                        </div>
                    </div>

                    <!-- Widgets de estado -->
                    <group class="bg-light rounded-3 p-3 mb-3">
                        <group>
                            <field name="prioridad" widget="priority"/>
                            <field name="origen_solicitud"/>
                        </group>
                        <group>
                            <field name="sla_limite_1" readonly="1" string="Límite SLA (horas)"/>
                            <field name="sla_cumplido" 
                                   invisible="estado != 'completado'"
                                   widget="boolean"/>
                            <field name="enable_whatsapp_notifications" widget="boolean_toggle"/>
                        </group>
                    </group>

                    <notebook>
                        <!-- TAB: INFORMACIÓN GENERAL -->
                        <page string="Información General" name="general">
                            <group class="p-2">
                                <group string="Máquina y Cliente">
                                    <field name="maquina_id" 
                                           readonly="estado != 'nuevo'"
                                           options="{'no_create': True}"/>
                                    <field name="cliente_id" readonly="1" force_save="1"/>
                                    <field name="modelo_maquina" readonly="1" force_save="1"/>
                                    <field name="serie_maquina" readonly="1" force_save="1"/>
                                    <field name="ip_maquina" readonly="1" force_save="1"/>
                                </group>
                                
                                <group string="Ubicación y Contacto">
                                    <field name="ubicacion" readonly="1" force_save="1"/>
                                    <field name="sede" readonly="1" force_save="1"/>
                                    <field name="contacto" readonly="estado in ('completado', 'cancelado')"/>
                                    <field name="telefono_contacto" readonly="estado in ('completado', 'cancelado')"/>
                                    <field name="correo" readonly="estado in ('completado', 'cancelado')"/>
                                </group>
                            </group>

                            <group string="Problema Reportado">
                                <field name="problema_reportado" 
                                       nolabel="1" 
                                       placeholder="Describa el problema reportado por el cliente..."
                                       readonly="estado in ('completado', 'cancelado')"/>
                            </group>
                        </page>

                        <!-- TAB: ASIGNACIÓN -->
                        <page string="Asignación" name="asignacion">
                            <group class="p-2">
                                <group string="Técnico">
                                    <field name="tecnico_id" 
                                           widget="many2one_avatar_user"
                                           readonly="estado != 'nuevo'"/>
                                    <field name="tecnico_respaldo_id" 
                                           widget="many2one_avatar_user"
                                           readonly="estado not in ('nuevo', 'asignado')"/>
                                    <field name="fecha_asignacion" readonly="1"/>
                                </group>
                                
                                <group string="Programación">
                                    <field name="fecha_programada" 
                                           readonly="estado != 'asignado'"/>
                                    <field name="duracion_estimada" 
                                           widget="float_time"
                                           readonly="estado not in ('nuevo', 'asignado')"/>
                                    <field name="fecha_confirmacion" readonly="1"/>
                                    <field name="fecha_inicio_ruta" readonly="1"/>
                                </group>
                            </group>
                            <group string="Vehículo" class="mt-2">
                                <group>
                                    <field name="vehicle_brand"
                                           readonly="estado in ('en_sitio', 'pausado', 'completado', 'cancelado')"/>
                                    <field name="vehicle_model"
                                           readonly="estado in ('en_sitio', 'pausado', 'completado', 'cancelado')"/>
                                </group>
                                <group>
                                    <field name="vehicle_plate"
                                           readonly="estado in ('en_sitio', 'pausado', 'completado', 'cancelado')"/>
                                    <field name="vehicle_info"
                                           readonly="1"
                                           invisible="not vehicle_info"/>
                                </group>
                            </group>
                        </page>

                        <!-- TAB: EJECUCIÓN DEL SERVICIO -->
                        <page string="Ejecución" 
                              name="ejecucion"
                              invisible="estado in ('nuevo', 'asignado', 'confirmado')">
                            <group class="p-2">
                                <group string="Tiempos">
                                    <field name="fecha_inicio" readonly="1" force_save="1"/>
                                    <field name="fecha_reanudacion" readonly="1" invisible="not fecha_reanudacion"/>
                                    <field name="fecha_fin" readonly="1" force_save="1"/>
                                    <field name="duracion_real" 
                                           widget="float_time" 
                                           readonly="1" 
                                           force_save="1"/>
                                </group>
                                
                                <group string="Métricas SLA">
                                    <field name="tiempo_respuesta" 
                                           widget="float_time" 
                                           readonly="1" 
                                           force_save="1"/>
                                    <field name="tiempo_resolucion" 
                                           widget="float_time" 
                                           readonly="1" 
                                           force_save="1"/>
                                </group>
                            </group>

                            <group string="Diagnóstico y solución" class="p-2">
                                <group>
                                    <field name="diagnostico"
                                           placeholder="Diagnóstico realizado por el técnico..."
                                           readonly="estado in ('completado', 'cancelado')"/>
                                    <field name="solucion_aplicada"
                                           readonly="estado in ('completado', 'cancelado')"/>
                                </group>
                                <group>
                                    <field name="trabajo_realizado"
                                           placeholder="Detalle el trabajo realizado..."
                                           readonly="estado in ('completado', 'cancelado')"/>
                                    <field name="observaciones_tecnico"
                                           placeholder="Observaciones adicionales..."
                                           readonly="estado in ('completado', 'cancelado')"/>
                                </group>
                            </group>

                            <group string="Insumos utilizados" class="p-2">
                                <field name="insumos_utilizados" 
                                       nolabel="1" 
                                       placeholder="Tóner, kits, piezas, etc..."
                                       readonly="estado in ('completado', 'cancelado')"/>
                            </group>
                        </page>

                        <!-- TAB: PEDIDO DE REPUESTOS -->
                        <page string="Repuestos"
                              name="repuestos"
                              invisible="estado not in ('en_sitio', 'pausado', 'completado') and not requiere_repuestos">
                            <div class="alert alert-light border d-flex align-items-start gap-2" role="alert">
                                <i class="fa fa-cogs fa-lg text-primary mt-1"/>
                                <div>
                                    Marque esta opción cuando el diagnóstico requiera partes o repuestos.
                                    La cotización se creará en borrador para revisión del área comercial.
                                </div>
                            </div>

                            <group class="p-2">
                                <group string="Necesidad técnica">
                                    <field name="requiere_repuestos"
                                           widget="boolean_toggle"
                                           readonly="estado not in ('en_sitio', 'pausado')"/>
                                    <field name="motivo_repuestos"
                                           placeholder="Ejemplo: Developer Cyan - Ricoh MP C2003 / MP C2503"
                                           readonly="estado not in ('en_sitio', 'pausado')"/>
                                </group>

                                <group string="Cotización comercial" invisible="not sale_order_id">
                                    <field name="sale_order_id" readonly="1"/>
                                    <field name="sale_order_state" widget="badge" readonly="1"/>
                                    <field name="sale_order_currency_id" invisible="1"/>
                                    <field name="sale_order_amount_total"
                                           widget="monetary"
                                           options="{'currency_field': 'sale_order_currency_id'}"
                                           readonly="1"/>
                                    <button name="action_open_parts_sale_order"
                                            type="object"
                                            string="Abrir cotización"
                                            icon="fa-external-link"
                                            class="btn-primary"/>
                                </group>
                            </group>

                            <div class="d-flex justify-content-end mt-2"
                                 invisible="not requiere_repuestos or sale_order_id or estado not in ('en_sitio', 'pausado')">
                                <button name="action_create_parts_sale_order"
                                        type="object"
                                        string="Crear pedido y agregar productos"
                                        icon="fa-shopping-cart"
                                        class="btn-primary"/>
                            </div>
                        </page>

                        <!-- TAB: CONTADORES -->
                        <page string="Contadores" 
                              name="contadores"
                              invisible="estado not in ('en_sitio', 'pausado', 'completado')">
                            <group>
                                <group string="Contadores al Servicio">
                                    <field name="contador_bn" 
                                           readonly="estado == 'completado'"/>
                                    <field name="contador_color" 
                                           readonly="estado == 'completado'"/>
                                    <field name="contador_total" 
                                           readonly="1" 
                                           force_save="1"/>
                                </group>
                            </group>

                            <div class="alert alert-info" role="alert">
                                <strong>ℹ️ Información:</strong> Los contadores se registrarán automáticamente al completar el servicio.
                            </div>
                        </page>

                        <!-- TAB: EVIDENCIAS -->
                        <page string="Evidencias" 
                              name="evidencias"
                              invisible="estado in ('nuevo', 'asignado', 'confirmado')">
                            <group>
                                <group string="Fotos">
                                    <field name="foto_antes" 
                                           widget="image" 
                                           class="oe_avatar"
                                           readonly="estado == 'completado'"/>
                                    <field name="foto_despues" 
                                           widget="image" 
                                           class="oe_avatar"
                                           readonly="estado == 'completado'"/>
                                </group>
                                
                                <group string="Conformidad">
                                    <field name="firma_cliente" 
                                           widget="image" 
                                           readonly="estado == 'completado'"/>
                                    <field name="nombre_firma" 
                                           readonly="estado == 'completado'"/>
                                    <field name="conformidad_cliente" 
                                           widget="boolean_toggle"
                                           readonly="estado == 'completado'"/>
                                </group>
                            </group>

                            <group string="Fotos Adicionales">
                                <field name="fotos_adicionales" 
                                       nolabel="1" 
                                       widget="many2many_binary"
                                       readonly="estado == 'completado'"/>
                            </group>
                        </page>

                        <!-- TAB: EVALUACIÓN -->
                        <page string="Evaluación" 
                              name="evaluacion"
                              invisible="estado != 'completado'">
                            <group>
                                <group>
                                    <field name="calificacion" widget="priority"/>
                                </group>
                            </group>
                            
                            <group string="Comentarios del Cliente">
                                <field name="comentario_cliente" 
                                       nolabel="1" 
                                       placeholder="Comentarios y sugerencias del cliente..."/>
                            </group>
                        </page>
                    </notebook>
                </sheet>

                <!-- Chatter -->
                <chatter/>
            </form>
        </field>
    </record>

    <!-- ========================================== -->
    <!-- VISTA KANBAN -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_kanban" model="ir.ui.view">
        <field name="name">copier.service.request.kanban</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <kanban default_group_by="estado" 
                    class="o_kanban_small_column" 
                    sample="1"
                    quick_create="false">
                
                <field name="name"/>
                <field name="cliente_id"/>
                <field name="modelo_maquina"/>
                <field name="tipo_problema_id"/>
                <field name="prioridad"/>
                <field name="estado"/>
                <field name="tecnico_id"/>
                <field name="fecha_programada"/>
                <field name="sla_limite_1"/>
                <field name="sla_cumplido"/>
                <field name="requiere_repuestos"/>
                <field name="sale_order_id"/>
                <field name="sale_order_state"/>
                <field name="color"/>
                <field name="activity_ids"/>
                <field name="activity_state"/>

                <templates>
                    <t t-name="card">
                        <div class="d-flex justify-content-between align-items-start gap-2">
                            <div>
                                <strong class="fs-5"><field name="name"/></strong>
                                <div class="text-muted"><field name="tipo_problema_id"/></div>
                            </div>
                            <field name="prioridad" widget="priority"/>
                        </div>

                        <div class="border-top mt-2 pt-2">
                            <div class="fw-semibold text-truncate">
                                <i class="fa fa-building-o me-2 text-muted"/>
                                <field name="cliente_id"/>
                            </div>
                            <div class="text-muted text-truncate mt-1">
                                <i class="fa fa-print me-2"/>
                                <field name="modelo_maquina"/>
                            </div>
                        </div>

                        <div class="d-flex flex-wrap gap-2 mt-3">
                            <span class="badge rounded-pill text-bg-warning"
                                  invisible="not requiere_repuestos or sale_order_id">
                                <i class="fa fa-exclamation-triangle me-1"/>Pedido pendiente
                            </span>
                            <span class="badge rounded-pill text-bg-info"
                                  invisible="not sale_order_id">
                                <i class="fa fa-shopping-cart me-1"/>
                                <field name="sale_order_id"/>
                            </span>
                        </div>

                        <div class="mt-3" invisible="not tecnico_id">
                            <field name="tecnico_id" widget="many2one_avatar_user"/>
                        </div>

                        <div class="small text-muted mt-2" invisible="not fecha_programada">
                            <i class="fa fa-calendar me-2"/>
                            <field name="fecha_programada"/>
                        </div>

                        <footer class="d-flex justify-content-between align-items-center mt-3 pt-2 border-top">
                            <field name="estado" widget="label_selection"
                                   options="{'classes': {'nuevo': 'default', 'asignado': 'info', 'confirmado': 'primary', 'en_ruta': 'warning', 'en_sitio': 'success', 'pausado': 'danger', 'completado': 'success', 'cancelado': 'default'}}"/>
                            <field name="activity_ids" widget="kanban_activity"/>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ========================================== -->
    <!-- VISTA DE BÚSQUEDA -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_search" model="ir.ui.view">
        <field name="name">copier.service.request.search</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <search string="Buscar servicios">
                <field name="name" string="Número"/>
                <field name="cliente_id"/>
                <field name="serie_maquina"/>
                <field name="modelo_maquina"/>
                <field name="tecnico_id"/>
                <field name="tipo_problema_id"/>
                <field name="sale_order_id" string="Pedido de repuestos"/>

                <separator/>
                <filter name="services_open"
                        string="Servicios abiertos"
                        domain="[('estado', 'not in', ['completado', 'cancelado'])]"/>
                <filter name="services_critical"
                        string="Prioridad crítica"
                        domain="[('prioridad', '=', '3')]"/>
                <filter name="parts_required"
                        string="Requieren repuestos"
                        domain="[('requiere_repuestos', '=', True)]"/>
                <filter name="parts_without_order"
                        string="Sin pedido creado"
                        domain="[('requiere_repuestos', '=', True), ('sale_order_id', '=', False)]"/>
                <filter name="services_completed"
                        string="Completados"
                        domain="[('estado', '=', 'completado')]"/>
                <filter name="services_cancelled"
                        string="Cancelados"
                        domain="[('estado', '=', 'cancelado')]"/>

                <separator/>
                <filter name="group_state" string="Agrupar por estado" context="{'group_by': 'estado'}"/>
                <filter name="group_technician" string="Agrupar por técnico" context="{'group_by': 'tecnico_id'}"/>
                <filter name="group_customer" string="Agrupar por cliente" context="{'group_by': 'cliente_id'}"/>
                <filter name="group_problem" string="Agrupar por problema" context="{'group_by': 'tipo_problema_id'}"/>
                <filter name="group_scheduled_day" string="Agrupar por fecha" context="{'group_by': 'fecha_programada:day'}"/>
            </search>
        </field>
    </record>

    <!-- ========================================== -->
    <!-- VISTA CALENDARIO -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_calendar" model="ir.ui.view">
        <field name="name">copier.service.request.calendar</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <calendar string="Servicios Programados" 
                      date_start="fecha_programada"
                      color="tecnico_id"
                      event_open_popup="true"
                      quick_create="false"
                      mode="month">
                
                <field name="name"/>
                <field name="cliente_id"/>
                <field name="modelo_maquina"/>
                <field name="tipo_problema_id"/>
                <field name="tecnico_id" 
                       filters="1" 
                       avatar_field="image_128"/>
                <field name="prioridad" 
                       filters="1"
                       invisible="1"/>
                <field name="estado" 
                       filters="1"
                       invisible="1"/>
            </calendar>
        </field>
    </record>

    <!-- ========================================== -->
    <!-- VISTA PIVOT (para reportes) -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_pivot" model="ir.ui.view">
        <field name="name">copier.service.request.pivot</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <pivot string="Análisis de Servicios" sample="1">
                <field name="create_date" interval="month" type="col"/>
                <field name="tecnico_id" type="row"/>
                <field name="tipo_problema_id" type="row"/>
                <field name="duracion_real" type="measure"/>
                <field name="tiempo_resolucion" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- ========================================== -->
    <!-- VISTA GRAPH -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_graph" model="ir.ui.view">
        <field name="name">copier.service.request.graph</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <graph string="Gráficos de Servicios" sample="1" type="bar">
                <field name="create_date" interval="week"/>
                <field name="estado" type="col"/>
            </graph>
        </field>
    </record>

    <!-- ========================================== -->
    <!-- VISTA ACTIVITY -->
    <!-- ========================================== -->
    <record id="view_copier_service_request_activity" model="ir.ui.view">
        <field name="name">copier.service.request.activity</field>
        <field name="model">copier.service.request</field>
        <field name="arch" type="xml">
            <activity string="Actividades de Servicio">
                <templates>
                    <div t-name="activity-box">
                        <div>
                            <field name="name"/>
                        </div>
                        <div>
                            <field name="cliente_id" muted="1"/>
                        </div>
                        <div>
                            <field name="tipo_problema_id"/>
                        </div>
                    </div>
                </templates>
            </activity>
        </field>
    </record>

   
    <!-- ========================================== -->
    <!-- VISTAS PARA TIPOS DE PROBLEMAS -->
    <!-- ========================================== -->
    
    <!-- List view -->
    <record id="view_copier_service_problem_type_list" model="ir.ui.view">
        <field name="name">copier.service.problem.type.list</field>
        <field name="model">copier.service.problem.type</field>
        <field name="arch" type="xml">
            <list string="Tipos de Problemas" editable="top">
                <field name="sequence" widget="handle"/>
                <field name="icono"/>
                <field name="name"/>
                
                
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_copier_service_problem_type_form" model="ir.ui.view">
        <field name="name">copier.service.problem.type.form</field>
        <field name="model">copier.service.problem.type</field>
        <field name="arch" type="xml">
            <form string="Tipo de Problema">
                <sheet>
                    <widget name="web_ribbon" title="Archivado" bg_color="text-bg-danger" invisible="active"/>
                    
                    <div class="oe_title">
                        <h1>
                            <field name="icono" class="me-2" style="font-size: 2em;"/>
                            <field name="name" placeholder="Nombre del tipo de problema..."/>
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="sequence"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                        
                    </group>

                    
                </sheet>
            </form>
        </field>
    </record>

</odoo>
