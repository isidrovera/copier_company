<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="pdf_view_template" name="PDF View">
        <t t-call="website.layout">
            <div class="container mt-5">
                <div class="row">
                    <div class="col-12">
                        <h1>Vista de PDF</h1>
                        <div>
                            <iframe id="pdf-iframe" t-att-src="'/web/static/lib/pdfjs/web/viewer.html?file=' + pdf_url + '#toolbar=0&amp;navpanes=0&amp;scrollbar=1'" width="100%" height="800px"></iframe>
                        </div>
                    </div>
                </div>
            </div>
            <script type="text/javascript">
                document.addEventListener('DOMContentLoaded', function() {
                    var iframe = document.getElementById('pdf-iframe');
                    iframe.onload = function() {
                        var iframeDoc = iframe.contentDocument || iframe.contentWindow.document;
                        var viewer = iframeDoc.getElementById('viewer');
                        if (viewer) {
                            // Disable download and print buttons
                            var downloadButton = iframeDoc.getElementById('download');
                            var printButton = iframeDoc.getElementById('print');
                            if (downloadButton) {
                                downloadButton.style.display = 'none';
                            }
                            if (printButton) {
                                printButton.style.display = 'none';
                            }
                        }
                    };
                });
            </script>
        </t>
    </template>
</odoo>
