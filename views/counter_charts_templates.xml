<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Template para la página de contadores con gráficos simples -->
<template id="portal_my_copier_counters" name="My Equipment Counters">
    <t t-call="portal.portal_layout">
        <t t-set="breadcrumbs_searchbar" t-value="True"/>
        
        <!-- Incluir Chart.js directamente -->
        <t t-set="head_elements">
            <link rel="stylesheet" type="text/css" href="/copier_company/static/src/css/counter_charts.css"/>
            <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.9.1/chart.min.js"></script>
        </t>
        
        <!-- Elemento con datos para los gráficos -->
        <div id="counter_chart_data" 
             t-att-data-chart-data="chart_data"
             t-att-data-is-color="'true' if equipment.tipo == 'color' else 'false'"
             style="display: none;"></div>
        
        <t t-call="portal.portal_searchbar">
            <t t-set="title">Historial de Lecturas</t>
        </t>
        
        <div class="row mt-4">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h4 class="mb-0">
                            <i class="fa fa-tachometer me-2"></i>Lecturas de la máquina 
                            <strong t-field="equipment.name.name"/> 
                            (Serie: <span t-field="equipment.serie_id"/>)
                        </h4>
                    </div>
                    <div class="card-body">
                        <t t-if="not counters">
                            <div class="alert alert-info text-center">
                                <p>No hay registros de lecturas para este equipo</p>
                            </div>
                        </t>
                        <t t-if="counters">
                            <!-- Gráficos de consumo -->
                            <div class="mb-4">
                                <h5>Gráficos de Consumo</h5>
                                <ul class="nav nav-tabs" id="graphTabs" role="tablist">
                                    <li class="nav-item" role="presentation">
                                        <button class="nav-link active" id="monthly-tab" data-bs-toggle="tab" data-bs-target="#monthly" type="button" role="tab" aria-controls="monthly" aria-selected="true">Consumo Mensual</button>
                                    </li>
                                    <li class="nav-item" role="presentation">
                                        <button class="nav-link" id="yearly-tab" data-bs-toggle="tab" data-bs-target="#yearly" type="button" role="tab" aria-controls="yearly" aria-selected="false">Consumo Anual</button>
                                    </li>
                                </ul>
                                <div class="tab-content p-3 border border-top-0 rounded-bottom" id="graphTabsContent">
                                    <div class="tab-pane fade show active" id="monthly" role="tabpanel" aria-labelledby="monthly-tab">
                                        <div class="chart-container">
                                            <canvas id="monthlyChart"></canvas>
                                        </div>
                                    </div>
                                    <div class="tab-pane fade" id="yearly" role="tabpanel" aria-labelledby="yearly-tab">
                                        <div class="chart-container">
                                            <canvas id="yearlyChart"></canvas>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Resto del template sigue igual... -->
                            <div class="table-responsive">
                                <!-- ... tu tabla existente ... -->
                            </div>
                            
                            <!-- Resumen de facturación -->
                            <!-- ... tu resumen existente ... -->
                        </t>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Botón para volver a la página de detalle del equipo -->
        <div class="text-center mt-4">
            <a t-att-href="'/my/copier/equipment/%s' % equipment.id" class="btn btn-secondary">
                <i class="fa fa-arrow-left me-1"></i>Volver al Equipo
            </a>
        </div>
        
        <!-- JavaScript para inicializar los gráficos -->
        <script type="text/javascript">
            document.addEventListener('DOMContentLoaded', function() {
                // Obtener datos para los gráficos
                var chartDataElement = document.getElementById('counter_chart_data');
                if (!chartDataElement) {
                    console.warn('No se encontraron datos para los gráficos');
                    return;
                }
                
                try {
                    var chartData = JSON.parse(chartDataElement.dataset.chartData || '{"monthly":[],"yearly":[]}');
                    var isColor = chartDataElement.dataset.isColor === 'true';
                    
                    // Inicializar gráfico mensual
                    initMonthlyChart(chartData.monthly, isColor);
                    
                    // Inicializar gráfico anual
                    initYearlyChart(chartData.yearly, isColor);
                } catch (error) {
                    console.error('Error al inicializar los gráficos:', error);
                }
            });
            
            function initMonthlyChart(data, isColor) {
                if (!data || data.length === 0 || !document.getElementById('monthlyChart')) {
                    return;
                }
                
                var ctx = document.getElementById('monthlyChart').getContext('2d');
                
                var datasets = [
                    {
                        label: 'Copias B/N',
                        data: data.map(function(item) { return item.bn; }),
                        backgroundColor: 'rgba(54, 162, 235, 0.6)',
                        borderColor: 'rgba(54, 162, 235, 1)',
                        borderWidth: 1
                    }
                ];
                
                if (isColor) {
                    datasets.push({
                        label: 'Copias Color',
                        data: data.map(function(item) { return item.color; }),
                        backgroundColor: 'rgba(255, 99, 132, 0.6)',
                        borderColor: 'rgba(255, 99, 132, 1)',
                        borderWidth: 1
                    });
                }
                
                new Chart(ctx, {
                    type: 'bar',
                    data: {
                        labels: data.map(function(item) { return item.name; }),
                        datasets: datasets
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            title: {
                                display: true,
                                text: 'Consumo Mensual de Copias'
                            },
                            legend: {
                                position: 'top',
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true,
                                title: {
                                    display: true,
                                    text: 'Número de copias'
                                }
                            },
                            x: {
                                title: {
                                    display: true,
                                    text: 'Mes'
                                }
                            }
                        }
                    }
                });
            }
            
            function initYearlyChart(data, isColor) {
                if (!data || data.length === 0 || !document.getElementById('yearlyChart')) {
                    return;
                }
                
                var ctx = document.getElementById('yearlyChart').getContext('2d');
                
                var datasets = [
                    {
                        label: 'Copias B/N',
                        data: data.map(function(item) { return item.bn; }),
                        backgroundColor: 'rgba(54, 162, 235, 0.6)',
                        borderColor: 'rgba(54, 162, 235, 1)',
                        borderWidth: 1
                    }
                ];
                
                if (isColor) {
                    datasets.push({
                        label: 'Copias Color',
                        data: data.map(function(item) { return item.color; }),
                        backgroundColor: 'rgba(255, 99, 132, 0.6)',
                        borderColor: 'rgba(255, 99, 132, 1)',
                        borderWidth: 1
                    });
                }
                
                new Chart(ctx, {
                    type: 'bar',
                    data: {
                        labels: data.map(function(item) { return item.name; }),
                        datasets: datasets
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            title: {
                                display: true,
                                text: 'Consumo Anual de Copias'
                            },
                            legend: {
                                position: 'top',
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true,
                                title: {
                                    display: true,
                                    text: 'Número de copias'
                                }
                            },
                            x: {
                                title: {
                                    display: true,
                                    text: 'Año'
                                }
                            }
                        }
                    }
                });
            }
        </script>
    </t>
</template>
</odoo>