<odoo>
  <data>
    <!-- List View -->
    <record model="ir.ui.view" id="copier_company.list">
      <field name="name">copier_company.list</field>
      <field name="model">copier.company</field>
      <field name="arch" type="xml">
        <list>
          <field name="secuencia"/>
          <field name="marca_id"/>
          <field name="name"/>
          <field name="serie_id"/>
          <field name="cliente_id"/>
          <field name="sede"/>
          <field name="ubicacion"/>
          <field name="estado_maquina_id"/>
          <button name="crear_ticket" string="Servicio" type="object" icon="fa-wrench" class="btn-primary"/>
          <button name="generar_qr_code" string="QR" type="object" icon="fa-qrcode" class="btn-info"/>
        </list>
      </field>
    </record>

    <!-- Form View -->
    <record id="view_maquinas_form" model="ir.ui.view">
      <field name="name">copier.company.form</field>
      <field name="model">copier.company</field>
      <field name="arch" type="xml">
        <form>
          <header>
            <button name="crear_ticket" string="Crear Ticket" type="object" class="btn-primary"/>
            <button name="generar_qr_code" string="Generar QR" type="object" class="btn-info"/>
            <button name="action_print_report" string="Imprimir Cotización" type="object" class="btn-success"/>
            <button name="enviar_correo_propuesta" string="Enviar Propuesta" type="object" class="oe_highlight"/>
            <button 
                    name="send_whatsapp_report" 
                    type="object" 
                    class="btn btn-success"
                    icon="fa-whatsapp"
                    string=" Enviar por WhatsApp"                    
                    style="background-color: #25D366 !important; border-color: #25D366 !important;"
                />
            <field name="estado_maquina_id" widget="statusbar" options="{'clickable': '1'}"/>
          </header>
          <sheet>
            <div class="alert alert-info">
              <strong>Instrucciones:</strong>
              <ul>
                <li>Complete los datos del equipo y cliente</li>
                <li>Configure el volumen mensual esperado</li>
                <li>Los montos se calculan automáticamente</li>
                <li>Genere el código QR para servicio técnico</li>
              </ul>
            </div>

            <group>
              <group string="Datos del Equipo">
                <field name="secuencia" readonly="1"/>
                <field name="marca_id" required="1"/>
                <field name="name" required="1"/>
                <field name="serie_id" required="1"/>
                <field name="tipo" required="1"/>
                <field name="accesorios_ids" widget="many2many_tags"/>
              </group>
              <group string="Datos del Cliente">
                <field name="cliente_id" required="1"/>
                <field name="tipo_identificacion"/>
                <field name="identificacion"/>
                <field name="sede" required="1"/>
                <field name="ubicacion" required="1"/>
              </group>
            </group>

            <notebook>
              <page name="facturacion" string="Facturación">
                <group>
                  <group string="Configuración">
                    <field name="currency_id"/>
                    <field name="fecha_inicio_alquiler" required="1"/>
                    <field name="duracion_alquiler_id" required="1"/>
                    <field name="fecha_fin_alquiler" readonly="1"/>
                    <field name="dia_facturacion"/>
                  </group>
                  <group string="Costos y Volúmenes">
                    <field name="costo_copia_bn"/>
                    <field name="costo_copia_color" invisible="tipo == 'monocroma'"/>
                    <field name="volumen_mensual_bn"/>
                    <field name="volumen_mensual_color" invisible="tipo == 'monocroma'"/>
                  </group>
                  <group string="Totales">
                    <field name="renta_mensual_bn"/>
                    <field name="renta_mensual_color" invisible="tipo == 'monocroma'"/>
                    <field name="igv"/>
                    <field name="descuento"/>
                    <field name="total_facturar_mensual" class="oe_subtotal_footer_separator"/>
                  </group>
                </group>
              </page>
              
              <page name="especificaciones" string="Especificaciones">
                <group>
                  <group>
                    <field name="especificaciones_id"/>
                  </group>
                  <group>
                    <field name="imagen_id"/>                    
                    <field name="qr_code" invisible="not qr_code"/>
                    
                  </group>
                </group>
              </page>
            </notebook>
          </sheet>
          <chatter />
        </form>
      </field>
    </record>

    <!-- Kanban View -->
    <record model="ir.ui.view" id="copier_company.kanban">
    <field name="name">copier_company.kanban</field>
    <field name="model">copier.company</field>
    <field name="arch" type="xml">
        <kanban default_group_by="estado_maquina_id" class="o_kanban_mobile" records_draggable="1">
            <field name="name"/>
            <field name="marca_id"/>
            <field name="serie_id"/>
            <field name="cliente_id"/>
            <field name="sede"/>
            <field name="ubicacion"/>
            <field name="estado_maquina_id"/>
            <field name="imagen_id"/>
            <field name="tipo"/>
            <field name="formato"/>
            
            <templates>
                <t t-name="kanban-box">
                    <div t-attf-class="oe_kanban_card oe_kanban_global_click position-relative rounded-3 shadow-sm" 
                         t-attf-style="background-color: #{record.tipo.raw_value === 'color' ? 'rgba(144, 238, 144, 0.1)' : 'rgba(200, 200, 200, 0.1)'}">
                        
                        <!-- Status Badge -->
                        <div class="position-absolute top-0 end-0 m-2">
                            <span t-attf-class="badge rounded-pill #{record.estado_maquina_id.raw_value === 'Disponible' ? 'bg-success' : record.estado_maquina_id.raw_value === 'En Uso' ? 'bg-primary' : 'bg-warning'}">
                                <field name="estado_maquina_id"/>
                            </span>
                        </div>

                        <div class="d-flex p-3">
                            <!-- Image Section -->
                            <div class="o_kanban_image me-3">
                                <field name="imagen_id" widget="image" class="rounded-3"/>
                            </div>

                            <!-- Content Section -->
                            <div class="flex-grow-1">
                                <!-- Header -->
                                <div class="d-flex align-items-center mb-2">
                                    <h3 class="mb-0 fw-bold text-primary">
                                        <field name="name"/>
                                    </h3>
                                    <small class="ms-2 text-muted">
                                        - <field name="serie_id"/>
                                    </small>
                                </div>

                                <!-- Details -->
                                <div class="mb-3">
                                    <div class="text-muted mb-2">
                                        <i class="fa fa-copyright me-2"/> <field name="marca_id"/>
                                    </div>
                                    
                                    <div class="d-flex flex-column gap-2">
                                        <div>
                                            <i class="fa fa-building-o me-2 text-primary"/> 
                                            <field name="cliente_id"/> 
                                            (<field name="sede"/>)
                                        </div>
                                        <div>
                                            <i class="fa fa-map-marker me-2 text-danger"/>
                                            <field name="ubicacion"/>
                                        </div>
                                        <div>
                                            <i class="fa fa-print me-2 text-success"/>
                                            <field name="tipo"/> - <field name="formato"/>
                                        </div>
                                    </div>
                                </div>

                                <!-- Action Buttons -->
                                <div class="d-flex gap-2 mt-3">
                                    <button name="crear_ticket" type="object" 
                                            class="btn btn-primary btn-sm d-flex align-items-center">
                                        <i class="fa fa-wrench me-1"/> Servicio
                                    </button>
                                    <button name="generar_qr_code" type="object" 
                                            class="btn btn-info btn-sm d-flex align-items-center text-white">
                                        <i class="fa fa-qrcode me-1"/> QR
                                    </button>
                                    <button name="enviar_correo_propuesta" type="object" 
                                            class="btn btn-success btn-sm d-flex align-items-center">
                                        <i class="fa fa-envelope me-1"/> Email
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </t>
            </templates>
        </kanban>
    </field>
</record>

    <!-- Search View -->
    <record id="copier_company_search" model="ir.ui.view">
      <field name="name">copier.company.search</field>
      <field name="model">copier.company</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="serie_id"/>
          <field name="cliente_id"/>
          <field name="sede"/>
          <separator/>
          <filter string="Disponibles" name="disponible" domain="[('estado_maquina_id.name','=','Disponible')]"/>
          <filter string="En Mantenimiento" name="mantenimiento" domain="[('estado_maquina_id.name','=','En Mantenimiento')]"/>
          <filter string="Fuera de Servicio" name="fuera_servicio" domain="[('estado_maquina_id.name','=','Fuera de Servicio')]"/>
          <group expand="0" string="Agrupar Por">
            <filter string="Estado" name="estado" context="{'group_by':'estado_maquina_id'}"/>
            <filter string="Cliente" name="cliente" context="{'group_by':'cliente_id'}"/>
            <filter string="Marca" name="marca" context="{'group_by':'marca_id'}"/>
          </group>
        </search>
      </field>
    </record>

    <!-- Action Window -->
    <record model="ir.actions.act_window" id="copier_company.action_window">
      <field name="name">Gestión de Máquinas</field>
      <field name="res_model">copier.company</field>
      <field name="view_mode">kanban,list,form</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
          ¡Registre su primera máquina!
        </p>
      </field>
    </record>

    <!-- Menu -->
    <menuitem name="Gestión de Fotocopiadoras" 
              id="copier_company.menu_root" 
              web_icon="copier_company,static/description/icon.png"/>

    <menuitem name="Máquinas" 
              id="copier_company.menu_1" 
              parent="copier_company.menu_root" 
              action="copier_company.action_window"/>
  </data>
</odoo>