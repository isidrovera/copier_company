<odoo>
    <record id="view_pcloud_config_form" model="ir.ui.view">
        <field name="name">pcloud.config.form</field>
        <field name="model">pcloud.config</field>
        <field name="arch" type="xml">
            <form string="pCloud Configuration">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="client_id"/>
                        <field name="client_secret"/>
                    </group>
                    <footer>
                        <button string="Connect" type="object" name="action_connect"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_pcloud_config" model="ir.actions.act_window">
        <field name="name">pCloud Configuration</field>
        <field name="res_model">pcloud.config</field>
        <field name="view_mode">form</field>
    </record>

    <menuitem id="menu_pcloud_config" name="pCloud"
              action="action_pcloud_config"
              parent="base.menu_configuration"/>

</odoo>
